---
phase: 18.1-false-green-bugfixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/engine/runner.ts
  - packages/core/src/engine/runner.test.ts
  - packages/core/src/assertions/schema.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "An argsSchema tool-call assertion validates successfully instead of failing with 'no JSON Schema validator was injected'"
    - "The injected validator is present on the main, per-turn, and command AssertionContexts"
    - "Core remains I/O-free (no fs/fetch/console added)"
  artifacts:
    - path: "packages/core/src/engine/runner.ts"
      provides: "AssertionContext.validateJsonSchema wired in all context-construction sites"
      contains: "validateJsonSchema"
  key_links:
    - from: "packages/core/src/engine/runner.ts"
      to: "AJV validator helper from assertions/schema.ts"
      via: "shared injected validate function"
      pattern: "validateJsonSchema"
---

<objective>
Fix ROADMAP success criterion #2: `buildAssertionContext` and the per-turn + command AssertionContexts in runner.ts never set `context.validateJsonSchema`, so every `argsSchema` tool-call assertion fails with "no JSON Schema validator was injected" — silently passing nothing or producing a misleading failure. Inject an AJV-backed validator reusing the exact AJV setup already in assertions/schema.ts (extract a shared helper rather than duplicating AJV config). Core must stay zero-I/O.

Purpose: argsSchema validation is a headline differentiator that is currently dead. This is an independent fix touching runner.ts (no schema.ts config-file overlap), so it runs in Wave 1.
Output: A shared AJV validator helper, wired into all three AssertionContext construction sites, with a regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18.1-false-green-bugfixes/18.1-CONTEXT.md
@.planning/research/v2.3.1-bugfix-verification.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract shared AJV validator helper from assertions/schema.ts (#2)</name>
  <files>packages/core/src/assertions/schema.ts</files>
  <read_first>
    - packages/core/src/assertions/schema.ts (the existing AJV instance + ajv-formats setup used by JSON Schema validation — this is the source of truth; do NOT duplicate its config)
    - packages/core/src/assertions/interface.ts (the AssertionContext type and the validateJsonSchema field signature it expects)
    - .planning/phases/18.1-false-green-bugfixes/18.1-CONTEXT.md (locked decision: reuse exact AJV setup, extract shared helper if cleaner, core stays I/O-free)
  </read_first>
  <behavior>
    - Test (RED first): the extracted helper, given a JSON Schema and a value, returns a validation result matching the AssertionContext.validateJsonSchema contract (success/failure with errors) — identical AJV behavior (formats enabled) to the existing schema.ts validator.
  </behavior>
  <action>
    Extract the AJV instantiation + ajv-formats wiring currently in schema.ts into a single reusable factory (e.g. a `createJsonSchemaValidator()` style pure function following the codebase `createXxx` factory convention) that returns a function matching the AssertionContext.validateJsonSchema signature defined in assertions/interface.ts. Keep schema.ts consuming this shared helper so there is exactly one AJV config. No new I/O — AJV is pure. Use `import type` for type-only imports and `.js` extensions. Colocate the helper test (or extend schema.test.ts).
  </action>
  <verify>
    <automated>cd /Users/petr/projects/kindlm && npx vitest run packages/core/src/assertions/schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: a single shared validator factory exists; schema.ts no longer instantiates AJV inline in two places (one config only).
    - Behavior: helper output matches the AssertionContext.validateJsonSchema contract with formats enabled.
    - `npx tsc --noEmit` and `npx eslint . --quiet` pass.
  </acceptance_criteria>
  <done>One shared AJV validator factory exists and is consumed by schema.ts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Inject validateJsonSchema into all AssertionContext sites in runner.ts (#2)</name>
  <files>packages/core/src/engine/runner.ts, packages/core/src/engine/runner.test.ts</files>
  <read_first>
    - packages/core/src/engine/runner.ts (buildAssertionContext + every other AssertionContext construction: per-turn context and command-test context)
    - packages/core/src/assertions/schema.ts (the shared validator factory created in Task 1)
    - packages/core/src/assertions/interface.ts (AssertionContext.validateJsonSchema field)
    - .planning/research/v2.3.1-bugfix-verification.md (evidence: argsSchema fails with 'no JSON Schema validator was injected')
  </read_first>
  <behavior>
    - Test (RED first): a tool-call assertion with an `argsSchema` run through the runner VALIDATES the args against the schema (passes for matching args, fails for non-matching) instead of failing with "no JSON Schema validator was injected".
    - Test: the validator is present on the main context, the per-turn context, and the command-test context (assert validateJsonSchema is defined in each).
  </behavior>
  <action>
    Set `validateJsonSchema` to the shared validator factory (from Task 1) on every AssertionContext constructed in runner.ts: `buildAssertionContext`, the per-turn AssertionContext, and the command-test AssertionContext. Instantiate the validator once and reuse the reference across sites. Do not add fs/fetch/console — AJV is pure, keeping core I/O-free. Add regression tests covering a matching-args pass and a mismatched-args fail, plus presence of validateJsonSchema on all three context sites.
  </action>
  <verify>
    <automated>cd /Users/petr/projects/kindlm && npx vitest run packages/core/src/engine/runner.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `validateJsonSchema` is assigned in all three AssertionContext construction sites in runner.ts.
    - Behavior: an argsSchema assertion validates real args; no "no JSON Schema validator was injected" error path is reachable for normal runs.
    - Test command: `npx vitest run packages/core/src/engine/runner.test.ts` passes including the RED-then-GREEN cases.
    - `npx tsc --noEmit` and `npx eslint . --quiet` pass; core has no new I/O imports.
  </acceptance_criteria>
  <done>argsSchema assertions validate correctly across all context sites; core stays I/O-free.</done>
</task>

</tasks>

<verification>
- `npx vitest run packages/core/src/engine/runner.test.ts packages/core/src/assertions/schema.test.ts` passes.
- `npx tsc --noEmit` passes; `npx eslint . --quiet` clean.
- Grep confirms no `fs`/`fetch`/`console` added to core for this change.
</verification>

<success_criteria>
ROADMAP #2: argsSchema tool-call assertions validate via an injected AJV validator on every AssertionContext; core remains zero-I/O.
</success_criteria>

<output>
Create `.planning/phases/18.1-false-green-bugfixes/18.1-02-SUMMARY.md` when done.
</output>
