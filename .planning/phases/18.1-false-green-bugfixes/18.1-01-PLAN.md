---
phase: 18.1-false-green-bugfixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/config/schema.ts
  - packages/core/src/config/schema.test.ts
  - packages/cli/src/commands/validate.ts
  - packages/cli/src/commands/validate.test.ts
  - packages/cli/src/utils/select-reporter.ts
  - packages/cli/src/utils/select-reporter.test.ts
  - packages/cli/src/commands/test.ts
  - packages/cli/src/commands/trace.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "An unknown/typo'd key under expect (e.g. tooCalls, contain) is REJECTED at validate time, not silently stripped"
    - "An existing valid config (incl. a tool-call with argsSchema) still round-trips through the strict parser successfully"
    - "validate command prints a did-you-mean suggestion naming the bad key for an unrecognized expect key"
    - "kindlm test --reporter json stamps the version threaded from the caller (KINDLM_VERSION), equal to packages/cli/package.json version, never a silent 0.0.0 in the wired path"
    - "trace.ts still compiles under the new selectReporter signature"
  artifacts:
    - path: "packages/core/src/config/schema.ts"
      provides: "Strict assertion-bearing expect sub-schemas rejecting unknown keys"
      contains: ".strict()"
    - path: "packages/cli/src/utils/select-reporter.ts"
      provides: "selectReporter(type, version?) threading version into createJsonReporter"
      contains: "createJsonReporter"
    - path: "packages/cli/src/commands/validate.test.ts"
      provides: "Gating test: typo'd expect key yields unrecognized_keys + suggestion"
      contains: "unrecognized_keys"
  key_links:
    - from: "packages/cli/src/commands/test.ts"
      to: "packages/cli/src/utils/select-reporter.ts"
      via: "selectReporter(options.reporter, KINDLM_VERSION)"
      pattern: "selectReporter\\("
    - from: "packages/cli/src/utils/select-reporter.ts"
      to: "createJsonReporter"
      via: "version argument passed through from caller"
      pattern: "createJsonReporter\\(version"
---

<objective>
Fix two independent false-green bugs (ROADMAP success criteria #1 and #4):
- #1: Strict-mode the assertion-bearing `expect` sub-schemas so unknown/typo'd keys are rejected at validate time instead of silently stripped (which produced tests with 0 assertions that "passed"). Add a "did you mean?" suggestion for `unrecognized_keys` in the validate command.
- #4: Thread the bundler-injected `KINDLM_VERSION` from the CALLER (`test.ts`) through `selectReporter` into `createJsonReporter()` so the JSON reporter stamps the real installed version (equal to packages/cli/package.json) instead of a silent "0.0.0".

Purpose: Both bugs make broken/misconfigured suites look healthy. They share no files except being CLI-surface fixes, so they parallelize cleanly and ship together.
Output: Strict Zod sub-schemas with a back-compat round-trip guard, a gating validate did-you-mean test, a caller-threaded version-stamped JSON reporter, and a guarantee that the second caller (`trace.ts`) still compiles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18.1-false-green-bugfixes/18.1-CONTEXT.md
@.planning/research/v2.3.1-bugfix-verification.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Strict-mode assertion-bearing expect sub-schemas with back-compat guard (#1)</name>
  <files>packages/core/src/config/schema.ts, packages/core/src/config/schema.test.ts</files>
  <read_first>
    - packages/core/src/config/schema.ts (the Zod sub-schemas; locate each z.object for the expect tree: output expect, each toolCalls entry incl. its parser-populated argsSchemaResolved field, judge expect, guardrails pii/keywords, baseline/drift, latency, cost)
    - packages/core/src/config/schema.test.ts (existing test patterns to extend)
    - templates/kindlm.yaml (representative valid config to use as the round-trip fixture; ensure it covers a tool-call with argsSchema or add such a fixture inline)
    - .planning/phases/18.1-false-green-bugfixes/18.1-CONTEXT.md (locked decision + exact file:line root cause for #1)
    - .planning/research/v2.3.1-bugfix-verification.md (runtime evidence: typo'd key silently stripped → 0 assertions)
  </read_first>
  <behavior>
    - Test (RED first): a config whose expect contains an unknown key (e.g. `tooCalls` typo or `contain`) FAILS Zod parse with a ZodError of code `unrecognized_keys` — currently it parses successfully and the key is dropped.
    - Test (back-compat, GATING): an existing valid config — templates/kindlm.yaml or a representative fixture that includes a tool-call entry carrying argsSchema — round-trips through the strict parser and still parses successfully.
    - Test: a tool-call expect carrying the parser-populated `argsSchemaResolved` field still parses successfully (strict must NOT reject this internal field).
    - Test: nested expect objects (output, guardrails.pii, guardrails.keywords, judge entries, toolCalls entries) each reject an unknown key.
  </behavior>
  <action>
    Before applying `.strict()`, grep/enumerate every field that the parser or interpolation step may inject onto each expect sub-schema — at minimum `argsSchemaResolved` on the ToolCallExpectSchema — and confirm each such field is declared as an allowed (optional) key on its schema so strict will not reject it. Then apply `.strict()` to every assertion-bearing `expect` sub-schema (the top-level expect object plus its nested objects: output expect, each toolCalls entry, each judge entry, guardrails.pii, guardrails.keywords, baseline.drift, latency, cost). Per the CONTEXT.md locked decision, add `argsSchemaResolved` as an optional field on ToolCallExpectSchema BEFORE calling `.strict()`. Do not strict-mode non-assertion config sections (providers/models/prompts) unless CONTEXT.md says so. Add the regression tests from behavior to schema.test.ts colocated, including the gating round-trip test of an existing valid config (incl. a tool-call with argsSchema).
  </action>
  <verify>
    <automated>cd /Users/petr/projects/kindlm && npx vitest run packages/core/src/config/schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: every assertion-bearing expect sub-schema in schema.ts ends with `.strict()`.
    - Behavior: a config with a typo'd expect key fails parse with `unrecognized_keys`; a config with `argsSchemaResolved` on a tool-call expect parses successfully.
    - GATING back-compat: a round-trip test of an existing valid config (templates/kindlm.yaml or representative fixture incl. a tool-call with argsSchema) through the strict parser passes — proving no allowed field was missed.
    - Test command: `npx vitest run packages/core/src/config/schema.test.ts` passes including the new RED-then-GREEN and round-trip cases.
    - `npx tsc --noEmit` and `npx eslint . --quiet` pass.
  </acceptance_criteria>
  <done>Unknown expect keys are rejected at validate time; argsSchemaResolved and all parser-injected fields still allowed; an existing valid config still round-trips; valid configs unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: Did-you-mean suggestion for unrecognized expect keys in validate command, with gating test (#1)</name>
  <files>packages/cli/src/commands/validate.ts, packages/cli/src/commands/validate.test.ts</files>
  <read_first>
    - packages/cli/src/commands/validate.ts (current error-rendering path for config parse failures)
    - .planning/phases/18.1-false-green-bugfixes/18.1-CONTEXT.md (canonical ref to the existing v2.2.0 suggestion / zod-path helper to reuse — DO NOT hand-roll a new one if a helper exists)
    - the existing suggestion/zod-path helper file identified in CONTEXT.md (reuse its closest-match / zod-path formatting functions)
  </read_first>
  <action>
    In the validate command's ZodError rendering, detect issues with code `unrecognized_keys` and, for each unknown key, emit a "did you mean '&lt;closest&gt;'?" hint using the EXISTING v2.2.0 suggestion helper (closest-match against the allowed sibling keys for that schema path) and the existing zod-path formatter referenced in CONTEXT.md. Reuse, do not duplicate. Keep output style consistent with the current validate error formatting (chalk usage already present). Core stays untouched — this is CLI presentation only. Add a colocated Vitest file validate.test.ts that runs validate against a config with a typo'd expect key (e.g. `tooCalls`) and asserts the rendered error both names the bad key as an `unrecognized_keys` issue AND prints a suggestion referencing the correct key (`toolCalls`).
  </action>
  <verify>
    <automated>cd /Users/petr/projects/kindlm && npx vitest run packages/cli/src/commands/validate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: validate.ts branches on `unrecognized_keys` and calls the reused suggestion helper (no new closest-match implementation introduced).
    - GATING test: validate.test.ts exists and contains a Vitest assertion that a typo'd key (`tooCalls`) produces an `unrecognized_keys` error naming the bad key AND a did-you-mean suggestion referencing `toolCalls`; the test fails if that behavior is absent (no `|| echo MISSING` fallback).
    - Test command: `npx vitest run packages/cli/src/commands/validate.test.ts` passes.
    - `npx tsc --noEmit` and `npx eslint . --quiet` pass.
  </acceptance_criteria>
  <done>validate surfaces a did-you-mean suggestion for typo'd expect keys, reusing the existing helper, proven by a gating colocated test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Thread caller version into JSON reporter; keep both callers compiling (#4)</name>
  <files>packages/cli/src/utils/select-reporter.ts, packages/cli/src/utils/select-reporter.test.ts, packages/cli/src/commands/test.ts, packages/cli/src/commands/trace.ts</files>
  <read_first>
    - packages/cli/src/utils/select-reporter.ts (current signature `selectReporter(type: string)` — NO version param; the `case "json"` returns `createJsonReporter()` with no arg)
    - packages/cli/src/commands/test.ts (line 25 `declare const KINDLM_VERSION: string;`; line ~201 `selectReporter(options.reporter)` — first caller, declares KINDLM_VERSION)
    - packages/cli/src/commands/trace.ts (lines ~225 `selectReporter(opts.reporter)` — SECOND caller; does NOT declare KINDLM_VERSION)
    - packages/core/src/reporters/json.ts (`createJsonReporter(version?: string)` — line 11 stamps `version ?? "0.0.0"`)
    - packages/cli/package.json (source of truth for the version that must be stamped)
    - .planning/research/v2.3.1-bugfix-verification.md (evidence: installed binary stamps 0.0.0)
  </read_first>
  <behavior>
    - Test (RED first): the JSON reporter, when selectReporter is called with a provided version string, stamps that exact version in its output rather than the default "0.0.0".
    - Test (single source of truth, GATING): read packages/cli/package.json version at test time, pass it as the version arg, and assert the JSON reporter output stamps exactly that value — proving the version flows caller → selectReporter → createJsonReporter and is not a hardcoded literal.
  </behavior>
  <action>
    Change the selectReporter signature to `selectReporter(type: string, version?: string)` and, in the `case "json"` branch, call `createJsonReporter(version)` so the value is threaded from the caller, NOT hardcoded inside select-reporter. Make `version` OPTIONAL so other callers still typecheck. Update the first caller test.ts (line ~201) to pass `KINDLM_VERSION`: `selectReporter(options.reporter, KINDLM_VERSION)` — test.ts already declares `declare const KINDLM_VERSION: string;` at line 25. For the second caller trace.ts (line ~225): trace.ts does NOT declare KINDLM_VERSION and does not produce a version-stamped JSON report the same way, so rely on the optional param and leave the trace.ts call unchanged (`selectReporter(opts.reporter)`). Do NOT add a hardcoded version literal anywhere. Verify trace.ts still compiles under the new signature via `npx tsc --noEmit`. Keep other reporter branches unchanged. Add colocated regression tests per behavior, including the package.json-equality gating test.
  </action>
  <verify>
    <automated>cd /Users/petr/projects/kindlm && npx vitest run packages/cli/src/utils/select-reporter.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `selectReporter(type: string, version?: string)` passes `version` into `createJsonReporter(version)` — the value originates from the caller, not a literal inside select-reporter.
    - Caller assertion: test.ts:~201 calls `selectReporter(options.reporter, KINDLM_VERSION)`.
    - Flow assertion (GATING): a test reading packages/cli/package.json version asserts the JSON reporter stamps exactly that value when given it — proving version flows caller → selectReporter → createJsonReporter, not satisfiable by a hardcode, and that the wired path no longer yields a silent "0.0.0".
    - Second-caller assertion: trace.ts compiles unchanged under the new optional-param signature (`npx tsc --noEmit` exits 0 across the workspace).
    - Test command: `npx vitest run packages/cli/src/utils/select-reporter.test.ts` passes.
    - `npx tsc --noEmit` exits 0 across the workspace and `npx eslint . --quiet` passes.
  </acceptance_criteria>
  <done>JSON reporter stamps the caller-threaded KINDLM_VERSION (equal to packages/cli/package.json) from a single source of truth; both selectReporter callers (test.ts, trace.ts) compile.</done>
</task>

</tasks>

<verification>
- `npx vitest run packages/core/src/config/schema.test.ts packages/cli/src/commands/validate.test.ts packages/cli/src/utils/select-reporter.test.ts` passes.
- `npx tsc --noEmit` exits 0 monorepo-wide (covers trace.ts under the new selectReporter signature).
- `npx eslint . --quiet` clean.
</verification>

<success_criteria>
ROADMAP #1: unknown/typo'd expect keys rejected at validate time with a did-you-mean hint; argsSchemaResolved and all parser-injected fields preserved; an existing valid config still round-trips.
ROADMAP #4: JSON reporter stamps the real installed version threaded from the caller and equal to packages/cli/package.json (never a silent 0.0.0 in the wired path); both selectReporter callers compile.
</success_criteria>

<output>
Create `.planning/phases/18.1-false-green-bugfixes/18.1-01-SUMMARY.md` when done.
</output>
