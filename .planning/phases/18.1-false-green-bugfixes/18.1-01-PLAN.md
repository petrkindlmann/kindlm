---
phase: 18.1-false-green-bugfixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/config/schema.ts
  - packages/core/src/config/schema.test.ts
  - packages/cli/src/commands/validate.ts
  - packages/cli/src/utils/select-reporter.ts
  - packages/cli/src/utils/select-reporter.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "An unknown/typo'd key under expect (e.g. tooCalls, contain) is REJECTED at validate time, not silently stripped"
    - "validate command prints a did-you-mean suggestion for an unrecognized expect key"
    - "Parser-populated argsSchemaResolved on a tool-call expect remains valid (not rejected by strict)"
    - "kindlm test --reporter json stamps the real installed binary version, never 0.0.0"
  artifacts:
    - path: "packages/core/src/config/schema.ts"
      provides: "Strict assertion-bearing expect sub-schemas rejecting unknown keys"
      contains: ".strict()"
    - path: "packages/cli/src/utils/select-reporter.ts"
      provides: "JSON reporter created with KINDLM_VERSION"
      contains: "KINDLM_VERSION"
  key_links:
    - from: "packages/cli/src/commands/validate.ts"
      to: "zod unrecognized_keys error"
      via: "suggestion helper for did-you-mean"
      pattern: "unrecognized_keys"
    - from: "packages/cli/src/utils/select-reporter.ts"
      to: "createJsonReporter"
      via: "version argument"
      pattern: "createJsonReporter\\("
---

<objective>
Fix two independent false-green bugs (ROADMAP success criteria #1 and #4):
- #1: Strict-mode the assertion-bearing `expect` sub-schemas so unknown/typo'd keys are rejected at validate time instead of silently stripped (which produced tests with 0 assertions that "passed"). Add a "did you mean?" suggestion for `unrecognized_keys` in the validate command.
- #4: Pass the bundler-injected `KINDLM_VERSION` into `createJsonReporter()` so the JSON reporter stamps the real installed version instead of "0.0.0".

Purpose: Both bugs make broken/misconfigured suites look healthy. They share no files except being CLI-surface fixes, so they parallelize cleanly and ship together.
Output: Strict Zod sub-schemas, validate-command suggestion output, version-stamped JSON reporter, colocated regression tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18.1-false-green-bugfixes/18.1-CONTEXT.md
@.planning/research/v2.3.1-bugfix-verification.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Strict-mode assertion-bearing expect sub-schemas (#1)</name>
  <files>packages/core/src/config/schema.ts, packages/core/src/config/schema.test.ts</files>
  <read_first>
    - packages/core/src/config/schema.ts (the Zod sub-schemas; locate each z.object for the expect tree: output expect, toolCalls expect, judge expect, guardrails pii/keywords, baseline/drift, latency, cost)
    - packages/core/src/config/schema.test.ts (existing test patterns to extend)
    - .planning/phases/18.1-false-green-bugfixes/18.1-CONTEXT.md (locked decision + exact file:line root cause for #1)
    - .planning/research/v2.3.1-bugfix-verification.md (runtime evidence: typo'd key silently stripped → 0 assertions)
  </read_first>
  <behavior>
    - Test (RED first): a config whose expect contains an unknown key (e.g. `tooCalls` typo or `contain`) FAILS Zod parse with a ZodError of code `unrecognized_keys` — currently it parses successfully and the key is dropped.
    - Test: a valid config with the correct keys still parses successfully (no regression).
    - Test: a tool-call expect carrying the parser-populated `argsSchemaResolved` field still parses successfully (strict must NOT reject this internal field).
    - Test: nested expect objects (output, guardrails.pii, guardrails.keywords, judge entries, toolCalls entries) each reject an unknown key.
  </behavior>
  <action>
    Apply `.strict()` to every assertion-bearing `expect` sub-schema (the top-level expect object plus its nested objects: output expect, each toolCalls entry, each judge entry, guardrails.pii, guardrails.keywords, baseline.drift, latency, cost). Per the CONTEXT.md locked decision, the ToolCallExpectSchema MUST still allow the parser-populated `argsSchemaResolved` field — add it as an optional field on that schema BEFORE calling `.strict()` so strict does not reject it. Do not strict-mode non-assertion config sections (providers/models/prompts) unless CONTEXT.md says so. Add the regression tests described in behavior to schema.test.ts colocated.
  </action>
  <verify>
    <automated>cd /Users/petr/projects/kindlm && npx vitest run packages/core/src/config/schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: every assertion-bearing expect sub-schema in schema.ts ends with `.strict()`.
    - Behavior: a config with a typo'd expect key fails parse with `unrecognized_keys`; a config with `argsSchemaResolved` on a tool-call expect parses successfully.
    - Test command: `npx vitest run packages/core/src/config/schema.test.ts` passes including the new RED-then-GREEN cases.
    - `npx tsc --noEmit` and `npx eslint . --quiet` pass.
  </acceptance_criteria>
  <done>Unknown expect keys are rejected at validate time; argsSchemaResolved still allowed; valid configs unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: Did-you-mean suggestion for unrecognized expect keys in validate command (#1)</name>
  <files>packages/cli/src/commands/validate.ts</files>
  <read_first>
    - packages/cli/src/commands/validate.ts (current error-rendering path for config parse failures)
    - .planning/phases/18.1-false-green-bugfixes/18.1-CONTEXT.md (canonical ref to the existing v2.2.0 suggestion / zod-path helper to reuse — DO NOT hand-roll a new one if a helper exists)
    - the existing suggestion/zod-path helper file identified in CONTEXT.md (reuse its closest-match / zod-path formatting functions)
  </read_first>
  <action>
    In the validate command's ZodError rendering, detect issues with code `unrecognized_keys` and, for each unknown key, emit a "did you mean '&lt;closest&gt;'?" hint using the EXISTING v2.2.0 suggestion helper (closest-match against the allowed sibling keys for that schema path) and the existing zod-path formatter referenced in CONTEXT.md. Reuse, do not duplicate. Keep output style consistent with the current validate error formatting (chalk usage already present). Core stays untouched — this is CLI presentation only.
  </action>
  <verify>
    <automated>cd /Users/petr/projects/kindlm && npx vitest run packages/cli/src/commands/validate.test.ts 2>/dev/null || echo "MISSING — add validate.test.ts coverage for unrecognized_keys suggestion"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: validate.ts branches on `unrecognized_keys` and calls the reused suggestion helper (no new closest-match implementation introduced).
    - CLI output: running validate on a config with `tooCalls` typo prints a did-you-mean line referencing `toolCalls`.
    - `npx tsc --noEmit` and `npx eslint . --quiet` pass.
  </acceptance_criteria>
  <done>validate surfaces a did-you-mean suggestion for typo'd expect keys, reusing the existing helper.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Stamp real version in JSON reporter (#4)</name>
  <files>packages/cli/src/utils/select-reporter.ts, packages/cli/src/utils/select-reporter.test.ts</files>
  <read_first>
    - packages/cli/src/utils/select-reporter.ts (the createJsonReporter() call missing the version arg)
    - packages/cli/src/commands/test.ts (where the bundler-injected KINDLM_VERSION constant is declared)
    - packages/core/src/reporters/json.ts (createJsonReporter signature / version parameter)
    - .planning/research/v2.3.1-bugfix-verification.md (evidence: installed binary stamps 0.0.0)
  </read_first>
  <behavior>
    - Test (RED first): the JSON reporter, when selected with a provided version string, stamps that version in its output rather than the default "0.0.0".
  </behavior>
  <action>
    Thread the bundler-injected `KINDLM_VERSION` constant (declared in test.ts) through to `select-reporter` and pass it as the version argument to `createJsonReporter()`. Single source of truth — do not introduce a second version literal. If select-reporter does not currently receive a version, add a version parameter to its factory signature and have the test command pass `KINDLM_VERSION`. Keep other reporter branches unchanged. Add colocated regression test asserting the stamped version equals the passed value.
  </action>
  <verify>
    <automated>cd /Users/petr/projects/kindlm && npx vitest run packages/cli/src/utils/select-reporter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: select-reporter.ts passes the version argument into `createJsonReporter(`.
    - Behavior: JSON reporter output carries the injected version, never "0.0.0", when a real version is provided.
    - Test command: `npx vitest run packages/cli/src/utils/select-reporter.test.ts` passes.
    - `npx tsc --noEmit` and `npx eslint . --quiet` pass.
  </acceptance_criteria>
  <done>JSON reporter stamps the real KINDLM_VERSION from a single source of truth.</done>
</task>

</tasks>

<verification>
- `npx vitest run packages/core/src/config/schema.test.ts packages/cli/src/utils/select-reporter.test.ts` passes.
- `npx tsc --noEmit` passes monorepo-wide.
- `npx eslint . --quiet` clean.
</verification>

<success_criteria>
ROADMAP #1: unknown/typo'd expect keys rejected at validate time with a did-you-mean hint; argsSchemaResolved preserved.
ROADMAP #4: JSON reporter stamps real installed version.
</success_criteria>

<output>
Create `.planning/phases/18.1-false-green-bugfixes/18.1-01-SUMMARY.md` when done.
</output>
