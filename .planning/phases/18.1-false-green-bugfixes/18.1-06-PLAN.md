---
phase: 18.1-false-green-bugfixes
plan: 06
type: execute
wave: 4
depends_on: ["18.1-01", "18.1-02", "18.1-03", "18.1-04", "18.1-05"]
files_modified:
  - .changeset/v2-3-1-false-green-bugfixes.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "A patch changeset exists describing the v2.3.1 false-green bugfixes for the published packages"
  artifacts:
    - path: ".changeset/v2-3-1-false-green-bugfixes.md"
      provides: "Changeset entry driving the v2.3.1 patch release"
      contains: "@kindlm/core"
  key_links:
    - from: ".changeset/v2-3-1-false-green-bugfixes.md"
      to: "Changesets release flow"
      via: "patch bump frontmatter"
      pattern: "patch"
---

<objective>
Author the Changesets entry for the v2.3.1 patch release covering the false-green bugfixes (#1-#6). The repo uses Changesets with automated version bump + publish on main; this plan only adds the changeset markdown — it does NOT run `changeset version` or publish (the release flow handles that). config.json links @kindlm/core and @kindlm/cli and ignores @kindlm/cloud.

Purpose: Ensures the bugfixes ship as v2.3.1 with an accurate, user-facing changelog entry. Runs last (Wave 4) so it can summarize all merged fixes.
Output: One changeset file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18.1-false-green-bugfixes/18.1-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add v2.3.1 patch changeset (#1-#6)</name>
  <files>.changeset/v2-3-1-false-green-bugfixes.md</files>
  <read_first>
    - .changeset/config.json (linked group [["@kindlm/core","@kindlm/cli"]], ignore ["@kindlm/cloud"], access public — confirm the bump targets the right packages)
    - packages/core/package.json, packages/cli/package.json (current versions 2.3.0 → patch = 2.3.1)
    - .planning/phases/18.1-false-green-bugfixes/18.1-CONTEXT.md (the 6 fixed items to summarize accurately)
  </read_first>
  <action>
    Create a changeset markdown file with frontmatter declaring a patch bump for @kindlm/core and @kindlm/cli (the linked published group; do not bump the ignored @kindlm/cloud or any private package). In the body, write a concise user-facing summary of the false-green bugfixes: strict expect-key validation with did-you-mean (#1), argsSchema validator injection (#2), opt-in tool-call ordering (#3), correct JSON-reporter version (#4), real PII detector framework (#5), and the --no-cache regression coverage (#6). Do NOT run `changeset version` or publish — the release workflow performs the bump. Match the existing changeset frontmatter format (quoted package keys mapped to patch).
  </action>
  <verify>
    <automated>cd /Users/petr/projects/kindlm && npx changeset status 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: a changeset markdown file exists declaring a patch bump for @kindlm/core and @kindlm/cli.
    - Behavior: `npx changeset status` reports the pending patch bump without error.
    - The body summarizes fixes #1-#6 accurately; no `changeset version`/publish was run.
  </acceptance_criteria>
  <done>A v2.3.1 patch changeset is staged for the release workflow.</done>
</task>

</tasks>

<verification>
- `npx changeset status` shows the pending patch bump for the published packages.
- Changeset frontmatter targets @kindlm/core and @kindlm/cli (linked group) at patch.
</verification>

<success_criteria>
The false-green bugfixes ship as v2.3.1 via a correctly-scoped patch changeset.
</success_criteria>

<output>
Create `.planning/phases/18.1-false-green-bugfixes/18.1-06-SUMMARY.md` when done.
</output>
