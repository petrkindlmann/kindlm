---
phase: 18.1-false-green-bugfixes
plan: 06
subsystem: release
tags: [changesets, release, v2.3.1]
requires: ["18.1-01", "18.1-02", "18.1-03", "18.1-04", "18.1-05"]
provides: ["v2.3.1 patch changeset"]
affects: ["@kindlm/core", "@kindlm/cli"]
key-files:
  created:
    - .changeset/v2-3-1-false-green-bugfixes.md
  modified: []
decisions:
  - "Patch bump scoped to the linked published group (@kindlm/core + @kindlm/cli); @kindlm/cloud is ignored and private packages excluded."
  - "Excluded the vscode package — these fixes are core/cli only."
  - "Did not run `changeset version`/publish; the release workflow performs the bump."
metrics:
  duration: ~4m
  completed: 2026-05-30
---

# Phase 18.1 Plan 06: v2.3.1 Changeset Summary

Added the Changesets patch entry that drives the v2.3.1 release, summarizing the false-green bugfixes (#1-#6) shipped across waves 1-3 as a single user-facing changelog block.

## What Was Done

- Created `.changeset/v2-3-1-false-green-bugfixes.md` with `patch` frontmatter for `@kindlm/core` and `@kindlm/cli` (both listed explicitly since they are linked in `config.json`). Linked + patch takes both 2.3.0 -> 2.3.1.
- Wrote a concise, human changelog body covering strict expect-key validation with did-you-mean (#1), argsSchema validator enforcement (#2), opt-in tool-call ordering (#3), the real version stamp in the JSON reporter (#4), and the configurable named PII detector registry with Luhn/mod-97 checks (#5). The #6 --no-cache fix was a regression test only (no user-facing behavior change) so it is not called out separately.
- Excluded `@kindlm/cloud` (ignored), private packages, and the vscode package (out of scope for these core/cli fixes).
- Did not run `changeset version` or publish — the automated release workflow on main handles the bump.

## Verification

`npx changeset status` output (the stray `/dev/tty` line is a harmless non-interactive warning):

```
🦋  info Packages to be bumped at patch:
🦋  - @kindlm/core
🦋  - @kindlm/cli
🦋  info NO packages to be bumped at minor
🦋  info NO packages to be bumped at major
```

Frontmatter package names verified to match `package.json` `name` fields exactly. No unexpected packages in the bump set.

## Deviations from Plan

None - plan executed exactly as written.

## Self-Check: PASSED

- FOUND: .changeset/v2-3-1-false-green-bugfixes.md
- FOUND: commit b34582f
