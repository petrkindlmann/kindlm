---
phase: 18.1-false-green-bugfixes
verified: 2026-05-30T10:15:00Z
status: passed
score: 6/6 must-haves verified
overrides_applied: 0
---

# Phase 18.1: v2.3.1 False-Green Bugfixes — Verification Report

**Phase Goal:** Eliminate every verified regression that produces a false-green test result, so a passing `kindlm` run can be trusted. Ships as a v2.3.1 patch.
**Verified:** 2026-05-30
**Status:** PASSED
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (per-criterion)

| # | Criterion | Implementing file(s) | Test file(s) | Verdict | Evidence |
|---|-----------|---------------------|--------------|---------|----------|
| 1 | Typo'd `expect` key → `validate` exits non-zero with `unrecognized_keys` naming the bad key | `core/config/schema.ts` `.strict()` on ExpectSchema:410, OutputExpectSchema:234, GuardrailsSchema:367, PIIGuardrailSchema:273, KeywordGuardrailSchema:288, JudgeCriterionSchema:309, ToolCallExpectSchema:344, BaselineDriftSchema:362, baseline/latency/cost inline:394/400/407; `cli/commands/validate.ts` did-you-mean (`withDidYouMean`, `suggestClosest`) | `config/schema.test.ts`, `config/schema.ordered.test.ts`, `cli/commands/validate.test.ts` | ✓ VERIFIED | 11 strict markers present. `argsSchemaResolved` retained on ToolCallExpectSchema (schema.ts:330) so parser back-compat holds. schema.test.ts:287 "rejects a typo'd top-level expect key (tooCalls) with unrecognized_keys" asserts `errors.some(e=>e.includes("tooCalls"))===true`; :367 "GATING back-compat: valid config with argsSchema still parses" + :391 "allows the parser-populated argsSchemaResolved field under strict" both assert `result.success===true` (valid round-trip). validate.test.ts:140 asserts error contains `tooCalls` and :143 contains "did you mean". 45+4+5 tests pass. |
| 2 | `argsSchema` with `additionalProperties:false` FAILS on leaked args, PASSES on permissive; never "no validator injected" | `core/engine/runner.ts` injects `validateJsonSchema` at ALL THREE sites: final context (line 406), per-turn context (line 495), command context (line 636); single shared AJV `createJsonSchemaValidator()` (line 24); `core/assertions/schema.ts` AJV factory; `tool-calls.ts` `evaluateArgsSchema` | `engine/runner.test.ts`, `assertions/json-schema-validator.test.ts` | ✓ VERIFIED | All 3 context sites confirmed by direct read. AJV reuses `{allErrors:true, strict:false}` + ajv-formats. runner.test.ts:799 "FAILS argsSchema (additionalProperties:false) on leaked/extra args", :815 "PASSES argsSchema on a permissive schema", :829 "injects validateJsonSchema into the main, per-turn, and command contexts", :879 "injects ... into the command-test context"; lines 812/874/911 assert the message "no JSON Schema validator was injected" is NOT present (proves the hole is closed). json-schema-validator.test.ts:36 "enforces additionalProperties:false (rejects extra args)". runner (24) + json-schema-validator (5) tests pass. |
| 3 | Opt-in ordered tool-call FAILS out of sequence; presence-only is default for plain list | `core/config/schema.ts` `toolCallsOrdered` (line 382); `core/assertions/registry.ts` `useOrdered = hasNumericOrder \|\| toolCallsOrdered===true` (line 35), synthesizes positions skipping shouldNotCall (lines 42-55); `tool-calls.ts` `createToolOrderAssertion` reused | `assertions/registry.test.ts`, `assertions/tool-calls.test.ts` | ✓ VERIFIED | All three behaviors tested in registry.test.ts: :152 "ordered opt-in PASSES when calls occur in declared order" (`every passed===true`), :168 "ordered opt-in FAILS when out of declared order" (asserts `TOOL_CALL_ORDER_WRONG`, refund-before-lookup), :192 "back-compat: plain list stays presence-only and passes regardless of order", :212 "numeric order still positional", :225 "synthesizes positions skipping shouldNotCall". 31+25 tests pass. |
| 4 | JSON + compliance report version equals package.json, npm-run and installed binary (no silent 0.0.0) | `core/reporters/json.ts` `version ?? "0.0.0"` (lines 5,11); `cli/utils/select-reporter.ts` threads `version` → `createJsonReporter(version)` (line 25); `cli/commands/test.ts` declares bundler `KINDLM_VERSION` (line 28), resolves it (lines 41-42), passes `CLI_VERSION` to `selectReporter` (line 229); `cli/commands/trace.ts` is the second caller (selectReporter line 225) and compiles | `cli/utils/select-reporter.test.ts`, `reporters/json.test.ts` | ✓ VERIFIED | Caller→selectReporter→createJsonReporter flow intact. test.ts threads KINDLM_VERSION; trace.ts also imports/calls selectReporter and compiles. select-reporter.test.ts reads real `packages/cli/package.json` (lines 11-14) and asserts `parsed.kindlm.version===PKG_VERSION` (:81) AND `!==`"0.0.0"` (:82) — cannot be satisfied by a hardcode. 5+4 tests pass. |
| 5 | PII covers documented configurable detectors (phone, IBAN+mod97, IP, JWT, API-key, undashed SSN); README honest; default preserves prior 3-pattern behavior | `core/assertions/pii.ts` registry (lines 96-140): ssn (dashed+undashed), credit_card (Luhn), email, phone (US+E.164), iban (mod-97), ip, jwt, api_key; `luhnValid`/`ibanMod97Valid` validators (lines 39-75); `DETECTOR_NAMES` (line 152) feeds schema enum; ReDoS guard `hasNestedQuantifiers` preserved (line 24, applied 212/234/266); default (no `detectors`) keeps legacy denyPatterns (line 196 branch) | `assertions/pii.test.ts` | ✓ VERIFIED | Negative checks: pii.test.ts:156 "rejects a Luhn-invalid card" (`luhnValid("...112")===false`), :167 "rejects a mod-97-invalid IBAN" (`ibanMod97Valid("...433")===false`), plus per-detector positive+negative pairs (:196-244). Back-compat: :252 "absent detectors runs legacy denyPatterns unchanged" with :260 asserting undashed SSN stays uncovered by legacy path. ReDoS guard :266. 28 tests pass. README:41 lists exactly `ssn, credit_card (Luhn), email, phone (US+E.164), iban (mod-97), ip, jwt, api_key` — matches what fires; README:121 documents `detectors:` list and back-compat default. |
| 6 | Two consecutive `--no-cache` runs vs counter-incrementing provider return distinct responses (regression; behavior already correct) | `cli/utils/init-adapters.ts` line 130 `if (!options.noCache) { adapters.set(name, createCachingAdapter(adapter)); }` — caching wrap skipped entirely under noCache (no read path; no code change needed) | `cli/utils/init-adapters.test.ts` | ✓ VERIFIED | init-adapters.test.ts:88 "bypasses cache reads when noCache is set: two identical calls return DISTINCT responses" asserts `first.text==="response-1"`, `second.text==="response-2"`, `second.text !== first.text`, `second.fromCache===undefined`. :106 contrast test proves caching ON replays the first response (`second.text==="response-1"`, `fromCache===true`). 2 tests pass. |

**Score:** 6/6 truths verified

### Out-of-Scope Check (#7 must NOT be present)

| Item | Status | Evidence |
|------|--------|----------|
| #7 flakiness / pass^k / pass@k / variance / flaky / bootstrap surfacing | ✓ CORRECTLY ABSENT | grep for `pass\^k\|passHatK\|pass_k\|flaky\|FLAKY\|variance\|bootstrap` across `packages/*/src/**/*.ts` excluding `.test.ts` returned **no matches** in non-test source. Belongs to Phase 19; CONTEXT.md `<deferred>` and ROADMAP both defer it. |

### Behavioral Spot-Checks (targeted vitest)

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Criteria #1,#3,#5,#6 suites | `vitest run schema.test.ts validate.test.ts tool-calls.test.ts registry.test.ts pii.test.ts select-reporter.test.ts init-adapters.test.ts` | 7 files, 141 tests passed (0 fail) | ✓ PASS |
| Criteria #2,#4 + ordered-schema suites | `vitest run runner.test.ts json-schema-validator.test.ts json.test.ts schema.ordered.test.ts` | 4 files, 37 tests passed (0 fail) | ✓ PASS |
| Changeset bump | `npx changeset status` | core (patch) + cli (patch) detected | ✓ PASS |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none) | — | No debt markers (TBD/FIXME/XXX) introduced in phase-modified files. `?? "0.0.0"` in json.ts is an intentional documented fallback, not a stub (real version is threaded by both callers). | ℹ️ Info | None |

### Human Verification Required

None. All criteria are programmatically verifiable and confirmed via source reads + passing tests. The "installed binary" half of #4 is covered by the `version === package.json` equality test (the tsup `define` of `KINDLM_VERSION` is the documented bundler mechanism; the test guards against divergence/`0.0.0`).

### Gaps Summary

No gaps. All six ROADMAP success criteria are implemented with substantive code, correctly wired, and each is backed by a non-hollow regression test that would catch the original false-green:

- **#1** applies `.strict()` to all assertion-bearing sub-schemas while preserving the parser-populated `argsSchemaResolved` key (verified by a passing valid-config round-trip), so no back-compat break.
- **#2** injects the AJV validator at all THREE AssertionContext construction sites (final, per-turn, command) — the previously identified false-green hole; tests assert both the FAIL-on-leaked-args behavior and the absence of the "no validator injected" message.
- **#3** ships `toolCallsOrdered` opt-in with passing/failing/presence-only tests including the refund-before-lookup wrong-order FAIL.
- **#4** threads the bundler version through caller→selectReporter→createJsonReporter from both `test.ts` and `trace.ts`; the equality test prevents a hardcoded pass.
- **#5** ships the named-detector registry with Luhn/mod-97 negative tests, preserves the ReDoS guard, and keeps default behavior unchanged; README is honest.
- **#6** confirmed-correct with a counter-provider regression test proving distinct responses under `--no-cache`.

#7 (flakiness/pass^k) is correctly out of scope and absent from non-test source.

---

_Verified: 2026-05-30_
_Verifier: Claude (gsd-verifier)_
