# Phase 18.1 — session scratchpad (survives truncation)

**Status as of 2026-05-30:** Phase scaffolded, CONTEXT.md written, planner about to be spawned.

## Done
- Verified all 7 external fix-plan items vs LOCAL source (npm 2.3.0 had drifted).
  Evidence: `.planning/research/v2.3.1-bugfix-verification.md`. Repro: `/tmp/kindlm-repro/repro.mjs`.
- Verdicts: #1-#5 real bugs; #6 already fixed (test only); #7 → Phase 19.
- Inserted Phase 18.1 "v2.3.1 False-Green Bugfixes" before Phase 19 in ROADMAP.md (list line ~67,
  detail line ~81), updated STATE.md pointers, roadmap-evolution entry added.
- Wrote `.planning/phases/18.1-false-green-bugfixes/18.1-CONTEXT.md` (authoritative, has file:line
  root causes + 6 success criteria + canonical refs). May or may not be committed (user controls commits).

## Decisions locked (from AskUserQuestion)
- Structure: dedicated bugfix phase BEFORE Phase 19 (v2.3.1 patch).
- Scope for #3 and #5: BROAD (full ordered/calls restructure; named-detector registry + locale packs + Luhn/mod-97).
- Implement ALL 7 (but #7 lands in Phase 19, #6 is test-only).

## Next steps
1. Spawn gsd-planner for 18.1 (skip research — investigation already done). Mode: standard, skip-research.
2. Run gsd-plan-checker loop (max 3 iters).
3. Then /gsd-execute-phase 18.1 with TDD per fix.
4. tsc --noEmit + eslint . --quiet before done (CLAUDE.md).

## Root-cause cheat sheet (verified)
- #1 schema.ts: 0 `.strict()` → add to Expect/ToolCallExpect/Guardrails/OutputExpect etc. Keep `argsSchemaResolved` allowed.
- #2 runner.ts buildAssertionContext (~388) + per-turn(~482) + command(~616) never set context.validateJsonSchema. Reuse AJV from assertions/schema.ts.
- #3 registry.ts:28 only orders when `order:` set. Add `ordered:true` opt-in, keep back-compat. createToolOrderAssertion already exists.
- #4 select-reporter.ts calls createJsonReporter() with no version → json.ts:11 "0.0.0". Pass KINDLM_VERSION.
- #5 pii.ts zero built-ins; only 3 Zod defaults fire. Build named-detector registry + detectors: config list.
- #6 init-adapters.ts:130 already skips cache wrap on noCache. Regression test only.

## Tasks (TaskCreate IDs)
#1 verify (completed), #2-#8 = fixes #1-#7. #8 (flakiness) → Phase 19, keep deferred.

## Gotchas this session
- Bash stdout intermittently renders empty — re-run or use ctx_execute(shell). Outputs DO happen.
- gsd-sdk: use global `gsd-sdk` (RUNTIME_DIR empty so local path check fails).
- phase.insert SDK rejected "Phase 18" (it's in collapsed v2.3.0 <details>) — inserted 18.1 manually instead.
- Provider enum: fake provider name rejected by Zod; repro uses `openai` with injected fake adapter.
