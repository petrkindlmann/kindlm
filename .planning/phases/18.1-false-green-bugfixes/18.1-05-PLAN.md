---
phase: 18.1-false-green-bugfixes
plan: 05
type: execute
wave: 3
depends_on: ["18.1-04"]
files_modified:
  - packages/core/src/assertions/pii.ts
  - packages/core/src/assertions/pii.test.ts
  - packages/core/src/config/schema.ts
  - packages/core/src/config/schema.test.ts
  - packages/core/src/assertions/registry.ts
  - README.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Named PII detectors actually fire: ssn (dashed+undashed), credit_card (Luhn-checked), email, phone (US+E.164), iban (mod-97), ip (v4), jwt, api_key (AKIA/sk-/ghp_/xox prefixes)"
    - "guardrails.pii accepts a detectors:[...] list selecting which named detectors run"
    - "Default detector set (no detectors: specified) matches the prior 3-pattern behavior for back-compat"
    - "The existing ReDoS nested-quantifier guard is preserved"
    - "README lists only the detectors that actually fire"
  artifacts:
    - path: "packages/core/src/assertions/pii.ts"
      provides: "Named-detector registry with Luhn + mod-97 + prefix detectors"
      contains: "credit_card"
    - path: "packages/core/src/config/schema.ts"
      provides: "Zod schema for guardrails.pii.detectors"
      contains: "detectors"
  key_links:
    - from: "packages/core/src/assertions/pii.ts"
      to: "detector registry"
      via: "detectors config selects named detectors"
      pattern: "detectors"
---

<objective>
Fix ROADMAP success criterion #5 (BROAD): `pii.ts` has zero built-in detectors — only the 3 Zod-default `denyPatterns` fire, so the documented PII coverage is fiction. Build a named-detector registry (ssn dashed+undashed, credit_card with Luhn check, email, phone US+E.164, iban mod-97, ip v4, jwt, api_key prefixes AKIA/sk-/ghp_/xox). Add a `detectors: [...]` config under `guardrails.pii` (default = current 3 for back-compat). Preserve the existing ReDoS nested-quantifier guard. Update README to list only what actually fires.

Purpose: Makes PII guardrails real instead of a false-green marketing claim.
Wave 3 because it edits config/schema.ts (conflicts with Plans 01 and 04 in earlier waves).
Output: detector registry, schema, tests, README.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18.1-false-green-bugfixes/18.1-CONTEXT.md
@.planning/research/v2.3.1-bugfix-verification.md
@packages/core/src/assertions/pii.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build named PII detector registry (#5)</name>
  <files>packages/core/src/assertions/pii.ts, packages/core/src/assertions/pii.test.ts</files>
  <read_first>
    - packages/core/src/assertions/pii.ts (current denyPatterns defaults, the hasNestedQuantifiers ReDoS guard at line 17-21, safeRegexExec, and the createPiiAssertion factory)
    - .planning/phases/18.1-false-green-bugfixes/18.1-CONTEXT.md (#5 locked detector list + Luhn/mod-97 requirements + back-compat default = current 3)
    - .planning/research/v2.3.1-bugfix-verification.md (#5 evidence: zero built-in detectors fire today)
  </read_first>
  <behavior>
    - Test (RED first) per detector — each fires on a positive sample and does NOT fire on a clean control:
    - ssn: matches both dashed (123-45-6789) and undashed (123456789) forms.
    - credit_card: matches a Luhn-VALID number; rejects a Luhn-invalid 16-digit string.
    - email: matches a standard address.
    - phone: matches US format and E.164 (+1...) format.
    - iban: matches a mod-97-VALID IBAN; rejects a mod-97-invalid one.
    - ip: matches an IPv4 address.
    - jwt: matches a three-segment base64url token.
    - api_key: matches AKIA, sk-, ghp_, xox prefixes.
    - Test: the ReDoS nested-quantifier guard still rejects/handles a pathological custom pattern (preserved behavior).
  </behavior>
  <action>
    Add a named-detector registry to pii.ts following the codebase factory convention (a record/map of detector name to a pure detector function or compiled-pattern entry; no classes). Implement: ssn (dashed + undashed), credit_card (regex candidate then Luhn checksum validation to cut false positives), email, phone (US + E.164), iban (regex candidate then mod-97 checksum), ip (v4), jwt (3 base64url segments), api_key (prefix set AKIA/sk-/ghp_/xox). Keep all detectors pure (core zero-I/O). Preserve the existing hasNestedQuantifiers ReDoS guard — route any user/custom patterns through it unchanged. Add colocated per-detector tests (positive + control) and a Luhn-invalid + mod-97-invalid negative case.
  </action>
  <verify>
    <automated>cd /Users/petr/projects/kindlm && npx vitest run packages/core/src/assertions/pii.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: pii.ts exports a named-detector registry including all 8 detectors; credit_card uses Luhn, iban uses mod-97.
    - Behavior: every detector fires on its positive sample and not on its control; Luhn-invalid and mod-97-invalid inputs do not match.
    - Source assertion: the hasNestedQuantifiers ReDoS guard is still present and applied to custom patterns.
    - Test command: `npx vitest run packages/core/src/assertions/pii.test.ts` passes.
    - `npx tsc --noEmit` and `npx eslint . --quiet` pass; no I/O added to core.
  </acceptance_criteria>
  <done>A working named-detector registry fires correctly with checksum validation and the ReDoS guard intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire detectors config into guardrails.pii schema + assertion (#5)</name>
  <files>packages/core/src/config/schema.ts, packages/core/src/config/schema.test.ts, packages/core/src/assertions/pii.ts, packages/core/src/assertions/registry.ts</files>
  <read_first>
    - packages/core/src/config/schema.ts (PIIGuardrailSchema at line 243 — note it is strict after Plan 01, so detectors must be a declared field)
    - packages/core/src/assertions/pii.ts (createPiiAssertion + the registry from Task 1 — consume the selected detectors)
    - packages/core/src/assertions/registry.ts (createPiiAssertion call site at line 71-78 — pass the new detectors through)
    - .planning/phases/18.1-false-green-bugfixes/18.1-CONTEXT.md (back-compat: default detector set = current 3-pattern behavior when detectors not specified)
  </read_first>
  <behavior>
    - Test (RED first): guardrails.pii accepts detectors: ["ssn","credit_card"] and only those named detectors run.
    - Test (back-compat): a guardrails.pii block with NO detectors field behaves exactly as before (the prior 3 default patterns fire).
    - Test: an unknown detector name in detectors:[...] is rejected by Zod (enum of valid names); the strict schema rejects an unknown sibling key.
  </behavior>
  <action>
    Add an optional detectors field to PIIGuardrailSchema as an array of a Zod enum of valid detector names (matching the registry keys from Task 1); declare it explicitly so strict mode accepts it. Default behavior when detectors is absent MUST replicate the current 3-pattern denyPatterns set (do not silently expand coverage for existing configs). Update the createPiiAssertion call site in registry.ts to pass the selected detectors. In createPiiAssertion, when detectors is present, run exactly the selected named detectors from the registry; otherwise keep the legacy default. Add the regression tests from behavior to schema.test.ts and pii.test.ts.
  </action>
  <verify>
    <automated>cd /Users/petr/projects/kindlm && npx vitest run packages/core/src/config/schema.test.ts packages/core/src/assertions/pii.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: PIIGuardrailSchema declares detectors as an enum array matching registry keys; createPiiAssertion runs selected detectors.
    - Behavior: explicit detectors list runs only those; absent detectors replicates the prior 3-pattern default; unknown detector name is rejected by Zod.
    - Test command: `npx vitest run packages/core/src/config/schema.test.ts packages/core/src/assertions/pii.test.ts` passes.
    - `npx tsc --noEmit` and `npx eslint . --quiet` pass.
  </acceptance_criteria>
  <done>guardrails.pii.detectors selects named detectors; default behavior is back-compatible.</done>
</task>

<task type="auto">
  <name>Task 3: Update README to list detectors that actually fire (#5)</name>
  <files>README.md</files>
  <read_first>
    - README.md (current PII / guardrails section claiming detector coverage)
    - packages/core/src/assertions/pii.ts (the registry from Task 1 — the source of truth for what actually fires)
    - .planning/phases/18.1-false-green-bugfixes/18.1-CONTEXT.md (#5 decision: README must list only detectors that actually fire, plus the configurable set)
  </read_first>
  <action>
    Replace any inaccurate PII coverage claims in README.md with an honest list of the named detectors that actually fire (ssn, credit_card, email, phone, iban, ip, jwt, api_key) and document the detectors:[...] config option plus the back-compat default. Do not overstate coverage. Keep wording aligned with the registry names from Task 1.
  </action>
  <verify>
    <automated>cd /Users/petr/projects/kindlm && grep -q "detectors" README.md && echo "README updated"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: README lists exactly the detectors implemented in pii.ts and documents the detectors config.
    - No coverage claim exceeds what the registry implements.
  </acceptance_criteria>
  <done>README accurately reflects the implemented PII detector set and configuration.</done>
</task>

</tasks>

<verification>
- `npx vitest run packages/core/src/assertions/pii.test.ts packages/core/src/config/schema.test.ts` passes.
- README mentions the detectors config and only the implemented detectors.
- `npx tsc --noEmit` and `npx eslint . --quiet` pass; no I/O added to core.
</verification>

<success_criteria>
ROADMAP #5: a documented, configurable PII detector set actually fires (with Luhn + mod-97 validation and the ReDoS guard); README lists only what fires.
</success_criteria>

<output>
Create `.planning/phases/18.1-false-green-bugfixes/18.1-05-SUMMARY.md` when done.
</output>
