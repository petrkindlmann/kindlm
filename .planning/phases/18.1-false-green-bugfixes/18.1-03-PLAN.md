---
phase: 18.1-false-green-bugfixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/cli/src/utils/init-adapters.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Two consecutive --no-cache runs against a counter-incrementing provider return DISTINCT responses (cache is bypassed)"
  artifacts:
    - path: "packages/cli/src/utils/init-adapters.test.ts"
      provides: "Regression test proving --no-cache skips cache-wrapping"
      contains: "noCache"
  key_links:
    - from: "packages/cli/src/utils/init-adapters.test.ts"
      to: "init-adapters noCache branch"
      via: "counter-incrementing fake provider"
      pattern: "noCache"
---

<objective>
Fix ROADMAP success criterion #6 (REGRESSION TEST ONLY — code already fixed): `init-adapters.ts` already skips cache-wrapping when `noCache` is set, but there is no test locking that behavior. Add a regression test asserting two consecutive `--no-cache` runs against a counter-incrementing fake provider return distinct responses.

Purpose: Prevent silent reintroduction of the cache-on-no-cache false-green bug. No production code changes — test-only, so it shares Wave 1 with zero file overlap.
Output: A colocated regression test in init-adapters.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18.1-false-green-bugfixes/18.1-CONTEXT.md
@.planning/research/v2.3.1-bugfix-verification.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regression test for --no-cache cache bypass (#6)</name>
  <files>packages/cli/src/utils/init-adapters.test.ts</files>
  <read_first>
    - packages/cli/src/utils/init-adapters.ts (the noCache branch that skips cache-wrapping — the behavior under test; DO NOT modify it)
    - packages/cli/src/utils/init-adapters.test.ts (existing test setup/mocks for adapters, if present)
    - .planning/research/v2.3.1-bugfix-verification.md (evidence for #6 and the already-applied fix)
    - .planning/phases/18.1-false-green-bugfixes/18.1-CONTEXT.md (locked decision: #6 is test-only, no code change)
  </read_first>
  <action>
    Add a Vitest regression test: build adapters with `noCache` enabled wrapping a fake provider whose `complete` returns a monotonically incrementing counter in its response text. Invoke the resulting adapter twice and assert the two responses DIFFER (proving no cache wrapping). Add a contrasting assertion (or comment) clarifying that with caching enabled the second call would return the cached first value. Follow existing test conventions (factory usage, `import type`, `.js` extensions). Do NOT touch init-adapters.ts production code.
  </action>
  <verify>
    <automated>cd /Users/petr/projects/kindlm && npx vitest run packages/cli/src/utils/init-adapters.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: test asserts two `--no-cache` invocations against a counter-incrementing provider return distinct values.
    - Source assertion: no diff to init-adapters.ts (test-only change).
    - Test command: `npx vitest run packages/cli/src/utils/init-adapters.test.ts` passes.
    - `npx tsc --noEmit` and `npx eslint . --quiet` pass.
  </acceptance_criteria>
  <done>A regression test locks the --no-cache cache-bypass behavior; no production code changed.</done>
</task>

</tasks>

<verification>
- `npx vitest run packages/cli/src/utils/init-adapters.test.ts` passes.
- `git diff --stat` shows only init-adapters.test.ts changed for this plan.
</verification>

<success_criteria>
ROADMAP #6: regression test proves --no-cache bypasses caching.
</success_criteria>

<output>
Create `.planning/phases/18.1-false-green-bugfixes/18.1-03-SUMMARY.md` when done.
</output>
