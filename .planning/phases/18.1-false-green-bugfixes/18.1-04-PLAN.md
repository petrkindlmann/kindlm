---
phase: 18.1-false-green-bugfixes
plan: 04
type: execute
wave: 2
depends_on: ["18.1-01"]
files_modified:
  - packages/core/src/config/schema.ts
  - packages/core/src/config/schema.test.ts
  - packages/core/src/assertions/registry.ts
  - packages/core/src/assertions/registry.test.ts
  - packages/vscode/src/completions.ts
  - packages/vscode/src/hover.ts
  - packages/vscode/schemas/kindlm.schema.json
  - templates/kindlm.yaml
  - docs/assertions.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "A toolCalls list with ordered:true enforces the declared call sequence (out-of-order sequence FAILS)"
    - "A plain toolCalls list (no ordered, no numeric order) remains presence-only and passes regardless of order (back-compat)"
    - "Numeric order: on entries remains positional as before (back-compat)"
    - "VS Code completions/hover/schema expose the ordered opt-in; template + docs document it"
  artifacts:
    - path: "packages/core/src/assertions/registry.ts"
      provides: "Builds order assertion when ordered opt-in is set, reusing createToolOrderAssertion"
      contains: "createToolOrderAssertion"
    - path: "packages/core/src/config/schema.ts"
      provides: "Zod schema for the ordered opt-in"
      contains: "ordered"
  key_links:
    - from: "packages/core/src/assertions/registry.ts"
      to: "createToolOrderAssertion"
      via: "ordered opt-in flag"
      pattern: "createToolOrderAssertion"
---

<objective>
Fix ROADMAP success criterion #3 (BROAD): `registry.ts` only builds the order assertion when a numeric `order:` is set; a plain `toolCalls` list is presence-only, so unsafe declared sequences pass. Add an `ordered: true` opt-in that enforces the declared sequence via the EXISTING `createToolOrderAssertion`, while preserving full back-compat (plain list = presence-only; numeric `order:` = positional). Update the Zod schema, registry wiring, VS Code schema/completions/hover, and the YAML template + docs.

Purpose: Closes a class of false-green where order-sensitive tool sequences are not actually checked.
Wave 2 because it edits config/schema.ts, which Plan 01 also edits (no two schema.ts plans in one wave).
Output: ordered opt-in across schema, registry, vscode, template, docs, with regression tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18.1-false-green-bugfixes/18.1-CONTEXT.md
@.planning/research/v2.3.1-bugfix-verification.md
@packages/core/src/config/schema.ts
@packages/core/src/assertions/registry.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ordered opt-in to Zod schema + registry wiring (#3)</name>
  <files>packages/core/src/config/schema.ts, packages/core/src/config/schema.test.ts, packages/core/src/assertions/registry.ts, packages/core/src/assertions/registry.test.ts</files>
  <read_first>
    - packages/core/src/config/schema.ts (toolCalls expect schema — pick the cleaner of object-form `ordered:true` vs a sibling field; remember Plan 01 made this schema strict, so the new key must be a declared field)
    - packages/core/src/assertions/registry.ts (createAssertionsFromExpect — the branch that currently only builds the order assertion for numeric `order:`)
    - packages/core/src/assertions/tool-calls.ts (createToolOrderAssertion — the EXISTING order assertion to reuse; do not write a new one)
    - .planning/phases/18.1-false-green-bugfixes/18.1-CONTEXT.md (locked back-compat rules: plain list unchanged, numeric order unchanged)
    - .planning/research/v2.3.1-bugfix-verification.md (evidence: plain toolCalls list is presence-only)
  </read_first>
  <behavior>
    - Test (RED first): a toolCalls block with `ordered: true` and a declared sequence FAILS when the actual tool calls occur out of declared order; PASSES when in order.
    - Test (back-compat): a plain toolCalls list with neither `ordered` nor numeric `order:` PASSES regardless of actual order (presence-only).
    - Test (back-compat): entries with numeric `order:` still enforce positional matching exactly as before.
    - Test: schema accepts the chosen `ordered` form and (because schema is strict) rejects an unknown sibling.
  </behavior>
  <action>
    Add the `ordered` opt-in to the toolCalls expect Zod schema — choose the cleaner shape (object-form with an `ordered: boolean` sibling to the list, or per-block flag) and declare it explicitly so strict mode accepts it. In registry.ts `createAssertionsFromExpect`, when the opt-in is set, build the order assertion using the existing `createToolOrderAssertion`, deriving the expected sequence from the declared toolCalls order. Leave the numeric-`order:` path and the plain-list presence-only path untouched. Add the regression tests from behavior to both schema.test.ts and registry.test.ts.
  </action>
  <verify>
    <automated>cd /Users/petr/projects/kindlm && npx vitest run packages/core/src/config/schema.test.ts packages/core/src/assertions/registry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: registry.ts builds the order assertion via `createToolOrderAssertion` when the ordered opt-in is set; schema.ts declares the `ordered` field.
    - Behavior: ordered:true enforces sequence; plain list and numeric order paths unchanged.
    - Test command: the two test files pass including RED-then-GREEN cases.
    - `npx tsc --noEmit` and `npx eslint . --quiet` pass.
  </acceptance_criteria>
  <done>ordered opt-in enforces declared sequence via the existing order assertion; back-compat preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Surface ordered opt-in in VS Code, template, and docs (#3)</name>
  <files>packages/vscode/src/completions.ts, packages/vscode/src/hover.ts, packages/vscode/schemas/kindlm.schema.json, templates/kindlm.yaml, docs/assertions.md</files>
  <read_first>
    - packages/vscode/schemas/kindlm.schema.json (toolCalls JSON schema block to extend with ordered)
    - packages/vscode/src/completions.ts (completion entries for toolCalls keys)
    - packages/vscode/src/hover.ts (hover docs for toolCalls keys)
    - templates/kindlm.yaml (example tests to add an ordered example)
    - docs/assertions.md (toolCalls assertion documentation)
    - packages/core/src/config/schema.ts (final shape of the ordered field from Task 1 — keep these in sync)
  </read_first>
  <action>
    Mirror the Task 1 `ordered` field into the VS Code JSON schema (kindlm.schema.json) so editor validation accepts it; add a completion entry for `ordered` in completions.ts and a hover description in hover.ts explaining the presence-only vs ordered vs numeric-order distinction. Add a commented `ordered: true` example to templates/kindlm.yaml and document the three modes (plain presence-only, ordered sequence, numeric positional) in docs/assertions.md. Keep wording consistent with the core schema field name chosen in Task 1.
  </action>
  <verify>
    <automated>cd /Users/petr/projects/kindlm && node -e "JSON.parse(require('fs').readFileSync('packages/vscode/schemas/kindlm.schema.json','utf8'));console.log('schema json valid')" && grep -q "ordered" templates/kindlm.yaml && grep -q "ordered" docs/assertions.md && grep -q "ordered" packages/vscode/src/completions.ts
  <acceptance_criteria>
    - JSON validity: kindlm.schema.json parses as valid JSON.
    - Source assertion: `ordered` appears in kindlm.schema.json, completions.ts, hover.ts, templates/kindlm.yaml, and docs/assertions.md.
    - Docs describe all three modes (plain presence-only, ordered sequence, numeric positional).
    - `npx tsc --noEmit` and `npx eslint . --quiet` pass.
  </acceptance_criteria>
  <done>VS Code, template, and docs document the ordered opt-in consistently with the core schema field.</done>
</task>

</tasks>

<verification>
- `npx vitest run packages/core/src/config/schema.test.ts packages/core/src/assertions/registry.test.ts` passes.
- kindlm.schema.json parses; `ordered` present in vscode + template + docs.
- `npx tsc --noEmit` and `npx eslint . --quiet` pass.
</verification>

<success_criteria>
ROADMAP #3: opt-in ordered tool-call expectation enforces declared sequence; presence-only and numeric-order paths preserved; editor + docs in sync.
</success_criteria>

<output>
Create `.planning/phases/18.1-false-green-bugfixes/18.1-04-SUMMARY.md` when done.
</output>
