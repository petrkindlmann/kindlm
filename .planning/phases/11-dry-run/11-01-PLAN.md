---
phase: 11-dry-run
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/providers/pricing.ts
  - packages/core/src/engine/test-plan.ts
  - packages/core/src/engine/test-plan.test.ts
autonomous: true
requirements:
  - DRY-04
must_haves:
  truths:
    - "buildTestPlan returns estimatedCostUsd per entry (null when pricing unknown)"
    - "buildTestPlan returns totalEstimatedCostUsd on the plan"
    - "Cost is computed as: (maxTokens / 1_000_000) * output_price_per_1M * repeat (output proxy)"
  artifacts:
    - path: "packages/core/src/providers/pricing.ts"
      provides: "KINDLM_PRICING consolidated table + estimateDryRunCost helper"
    - path: "packages/core/src/engine/test-plan.ts"
      provides: "TestPlanEntry.estimatedCostUsd + TestPlan.totalEstimatedCostUsd"
    - path: "packages/core/src/engine/test-plan.test.ts"
      provides: "Unit tests for cost estimation in buildTestPlan"
  key_links:
    - from: "packages/core/src/engine/test-plan.ts"
      to: "packages/core/src/providers/pricing.ts"
      via: "estimateDryRunCost(modelId, maxTokens, repeat)"
      pattern: "estimateDryRunCost"
---

<objective>
Add cost estimation to buildTestPlan in core so each TestPlanEntry carries an
estimated cost, and the TestPlan carries a total.

Purpose: Dry-run output (DRY-04) must show estimated cost per test and total.
This is the only requirement not yet implemented — DRY-01/02/03/05 are all live.

Output: Updated TestPlanEntry + TestPlan types with cost fields; KINDLM_PRICING
consolidated table in pricing.ts; estimateDryRunCost helper; unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Current TestPlanEntry (packages/core/src/engine/test-plan.ts) -->
```typescript
export interface TestPlanEntry {
  testName: string;
  modelId: string;
  provider: string;
  repeat: number;
  assertionTypes: string[];
  isCommand: boolean;
  skip: boolean;
  tags: string[];
  // MISSING: estimatedCostUsd: number | null
}

export interface TestPlan {
  suiteName: string;
  suiteDescription: string | undefined;
  project: string;
  entries: TestPlanEntry[];
  totalExecutionUnits: number;
  concurrency: number;
  timeoutMs: number;
  // MISSING: totalEstimatedCostUsd: number | null
}
```

<!-- Current pricing.ts only has lookupModelPricing(model, table) — no consolidated table -->
<!-- Per-provider private pricing tables live in: -->
<!--   openai.ts: gpt-4o $2.5/$10, gpt-4o-mini $0.15/$0.6, gpt-4-turbo $10/$30, o3-mini $1.1/$4.4 -->
<!--   anthropic.ts: claude-opus-4-5, claude-sonnet-4-5, claude-haiku-4-5 -->
<!--   gemini.ts: GEMINI_PRICING (internal) -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add KINDLM_PRICING and estimateDryRunCost to pricing.ts</name>
  <files>packages/core/src/providers/pricing.ts</files>
  <read_first>
    packages/core/src/providers/pricing.ts
    packages/core/src/providers/openai.ts (lines 14-19 — OPENAI_PRICING values)
    packages/core/src/providers/anthropic.ts (lines 14-18 — ANTHROPIC_PRICING values)
    packages/core/src/providers/gemini.ts (lines 14-25 — GEMINI_PRICING values)
    packages/core/src/providers/mistral.ts (lines 1-30 — check for MISTRAL_PRICING)
    packages/core/src/providers/cohere.ts (lines 1-30 — check for COHERE_PRICING)
  </read_first>
  <behavior>
    - KINDLM_PRICING is a Record<string, ModelPricing> combining all known provider models
    - estimateDryRunCost(modelId, maxTokens, repeat) returns number | null
    - When model not found in KINDLM_PRICING → returns null
    - Cost formula: (maxTokens / 1_000_000) * output_price_per_1M * repeat
      (We use output price as proxy since dry-run doesn't know actual token split)
    - estimateDryRunCost("gpt-4o", 1024, 1) → (1024/1_000_000) * 10.0 * 1 = 0.00001024
    - estimateDryRunCost("unknown-model", 1024, 1) → null
    - estimateDryRunCost("gpt-4o", 1024, 3) → 0.00001024 * 3 = 0.00003072
    - Command entries (modelId === "command") → null
  </behavior>
  <action>
    Add to packages/core/src/providers/pricing.ts (after existing exports):

    1. Export KINDLM_PRICING: consolidate pricing from all adapters into one table.
       Copy the key-value entries from OPENAI_PRICING, ANTHROPIC_PRICING, GEMINI_PRICING,
       and any others found. Do NOT modify the per-adapter private tables — keep them
       as-is. KINDLM_PRICING is a separate export for dry-run use.

    2. Export estimateDryRunCost(modelId: string, maxTokens: number, repeat: number): number | null
       - If modelId === "command" return null
       - Use lookupModelPricing(modelId, KINDLM_PRICING) to get price
       - If match.ok === false return null
       - Return (maxTokens / 1_000_000) * match.price.output * repeat

    Write the test file packages/core/src/providers/pricing.test.ts with the
    cases from the <behavior> block FIRST, run them (they must fail), then implement.
  </action>
  <verify>
    <automated>cd /Users/petr/projects/kindlm && npx vitest run packages/core/src/providers/pricing.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "KINDLM_PRICING" packages/core/src/providers/pricing.ts shows an exported const
    - grep -n "estimateDryRunCost" packages/core/src/providers/pricing.ts shows an exported function
    - grep -n "gpt-4o" packages/core/src/providers/pricing.ts shows the entry exists in KINDLM_PRICING
    - All vitest cases in pricing.test.ts pass
  </acceptance_criteria>
  <done>pricing.ts exports KINDLM_PRICING and estimateDryRunCost; all pricing tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add cost fields to TestPlanEntry/TestPlan and populate in buildTestPlan</name>
  <files>packages/core/src/engine/test-plan.ts, packages/core/src/engine/test-plan.test.ts</files>
  <read_first>
    packages/core/src/engine/test-plan.ts
    packages/core/src/types/config.ts (or packages/core/src/config/schema.ts — find ModelConfig.params.maxTokens)
  </read_first>
  <behavior>
    - TestPlanEntry gains: estimatedCostUsd: number | null
    - TestPlan gains: totalEstimatedCostUsd: number | null
    - For skipped entries, estimatedCostUsd is null
    - For command entries, estimatedCostUsd is null
    - For model entries: estimatedCostUsd = estimateDryRunCost(modelId, maxTokens, repeat)
      where maxTokens comes from modelConfig.params.maxTokens (fall back to 1024 if undefined)
    - totalEstimatedCostUsd: sum of all non-null estimatedCostUsd values;
      null only if ALL entries are null (no priceable entries)
    - buildTestPlan with a gpt-4o model (maxTokens=1024, repeat=1) returns
      entry.estimatedCostUsd ≈ 0.00001024
    - buildTestPlan with only command tests returns totalEstimatedCostUsd = null
  </behavior>
  <action>
    1. Add estimatedCostUsd: number | null to TestPlanEntry interface
    2. Add totalEstimatedCostUsd: number | null to TestPlan interface
    3. Import estimateDryRunCost from "../providers/pricing.js"
    4. In the model-entry loop: look up modelConfig.params.maxTokens (default 1024),
       call estimateDryRunCost(modelConfig.id, maxTokens, repeat), assign to entry
    5. After building entries, compute totalEstimatedCostUsd:
       - Collect all non-null estimatedCostUsd values from non-skipped entries
       - If array is empty → null, else → sum

    Write tests in packages/core/src/engine/test-plan.test.ts covering the
    behavior cases above. Run RED first, then implement GREEN.
  </action>
  <verify>
    <automated>cd /Users/petr/projects/kindlm && npx vitest run packages/core/src/engine/test-plan.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "estimatedCostUsd" packages/core/src/engine/test-plan.ts shows field in interface AND assignment in function
    - grep -n "totalEstimatedCostUsd" packages/core/src/engine/test-plan.ts shows field in TestPlan
    - grep -n "estimateDryRunCost" packages/core/src/engine/test-plan.ts shows import and usage
    - All vitest test-plan tests pass
    - npx tsc --noEmit exits 0 in packages/core
  </acceptance_criteria>
  <done>
    TestPlanEntry.estimatedCostUsd and TestPlan.totalEstimatedCostUsd populated;
    unit tests pass; TypeScript clean
  </done>
</task>

</tasks>

<verification>
cd /Users/petr/projects/kindlm && npm run typecheck 2>&1 | tail -20
cd /Users/petr/projects/kindlm && npx vitest run packages/core/src/engine/test-plan.test.ts packages/core/src/providers/pricing.test.ts 2>&1 | tail -20
</verification>

<success_criteria>
- KINDLM_PRICING and estimateDryRunCost exported from packages/core/src/providers/pricing.ts
- TestPlanEntry has estimatedCostUsd: number | null field with correct values
- TestPlan has totalEstimatedCostUsd: number | null field
- All new unit tests pass
- TypeScript strict-mode clean across workspace
</success_criteria>

<output>
After completion, create .planning/phases/11-dry-run/11-01-SUMMARY.md
</output>
