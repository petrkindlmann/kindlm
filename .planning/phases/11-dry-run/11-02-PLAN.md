---
phase: 11-dry-run
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - packages/cli/src/utils/dry-run.ts
  - packages/cli/src/utils/dry-run.test.ts
autonomous: true
requirements:
  - DRY-01
  - DRY-02
  - DRY-03
  - DRY-04
  - DRY-05
must_haves:
  truths:
    - "Running `kindlm test --dry-run` makes zero provider API calls"
    - "Output lists each test name, model(s), and repeat count"
    - "Output lists assertion types configured per test"
    - "Output shows estimated cost per test (or 'unknown' when not in pricing table)"
    - "Output shows total estimated cost or 'unknown' when no priceable models"
    - "Output shows total API call count (totalExecutionUnits)"
  artifacts:
    - path: "packages/cli/src/utils/dry-run.ts"
      provides: "formatTestPlan with cost columns"
      contains: "estimatedCostUsd"
    - path: "packages/cli/src/utils/dry-run.test.ts"
      provides: "Unit tests for formatTestPlan output including cost"
  key_links:
    - from: "packages/cli/src/utils/dry-run.ts"
      to: "packages/core/src/engine/test-plan.ts"
      via: "TestPlan.totalEstimatedCostUsd + TestPlanEntry.estimatedCostUsd"
      pattern: "totalEstimatedCostUsd"
---

<objective>
Update formatTestPlan in the CLI to display estimated cost per test and totals,
completing the dry-run feature for all five requirements (DRY-01 through DRY-05).

Purpose: DRY-04 data is now in TestPlan from Plan 01. This plan wires it to output.

Output: formatTestPlan renders cost column; integration verification confirms
`kindlm test --dry-run` produces the full expected output with no API calls.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-dry-run/11-01-SUMMARY.md
</context>

<interfaces>
<!-- Updated TestPlanEntry from Plan 01 -->
```typescript
export interface TestPlanEntry {
  testName: string;
  modelId: string;
  provider: string;
  repeat: number;
  assertionTypes: string[];
  isCommand: boolean;
  skip: boolean;
  tags: string[];
  estimatedCostUsd: number | null;   // NEW from Plan 01
}

export interface TestPlan {
  suiteName: string;
  suiteDescription: string | undefined;
  project: string;
  entries: TestPlanEntry[];
  totalExecutionUnits: number;
  concurrency: number;
  timeoutMs: number;
  totalEstimatedCostUsd: number | null;  // NEW from Plan 01
}
```

<!-- Current formatTestPlan output (packages/cli/src/utils/dry-run.ts) -->
<!-- Per-entry line: "  ✓ {testName} [{modelId}] x{repeat} ({tags}) -> {assertionTypes}" -->
<!-- Summary: Total execution units, Concurrency, Timeout -->
<!-- MISSING: per-entry cost + total cost in summary -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cost display to formatTestPlan</name>
  <files>packages/cli/src/utils/dry-run.ts, packages/cli/src/utils/dry-run.test.ts</files>
  <read_first>
    packages/cli/src/utils/dry-run.ts
    packages/core/src/engine/test-plan.ts (for TestPlanEntry/TestPlan types)
  </read_first>
  <behavior>
    Per-entry cost rendering:
    - entry.estimatedCostUsd is a number → append chalk.dim(` ~$0.000010`) (6 sig figs, no trailing zeros)
      Format: `~$` + cost.toPrecision(4) when cost >= 0.0001, or cost.toFixed(8) for tiny values
      Simpler approach: use cost.toFixed(6) for all values — consistent and readable
    - entry.estimatedCostUsd is null → append chalk.dim(" ~$?")
    - command entries → no cost suffix (they already show "[command]")

    Summary section additions (after Total execution units):
    - If plan.totalEstimatedCostUsd is not null:
        "  Estimated cost: ~$X.XXXXXX" (toFixed(6))
    - If plan.totalEstimatedCostUsd is null:
        "  Estimated cost: unknown (model pricing not found)"

    formatTestPlan called with a plan where entry.estimatedCostUsd = 0.00001024:
    → the entry line contains "~$0.000010"
    formatTestPlan called with plan.totalEstimatedCostUsd = 0.00001024:
    → summary contains "Estimated cost: ~$0.000010"
    formatTestPlan called with plan.totalEstimatedCostUsd = null:
    → summary contains "Estimated cost: unknown"
  </behavior>
  <action>
    1. In the per-entry loop in formatTestPlan, after assertionLabel, add costLabel:
       - If entry.isCommand → costLabel = ""
       - Else if entry.estimatedCostUsd !== null → costLabel = chalk.dim(` ~$${entry.estimatedCostUsd.toFixed(6)}`)
       - Else → costLabel = chalk.dim(" ~$?")
       Append costLabel to the push line.

    2. In the summary section (after "Total execution units: ..."), add:
       if plan.totalEstimatedCostUsd !== null:
         lines.push(`  Estimated cost: ${chalk.bold(`~$${plan.totalEstimatedCostUsd.toFixed(6)}`)}`);
       else:
         lines.push(`  Estimated cost: ${chalk.dim("unknown (model pricing not found)")}`);

    Write tests in dry-run.test.ts covering behavior cases above (RED first).
    Mock TestPlan objects directly — no file I/O needed.
  </action>
  <verify>
    <automated>cd /Users/petr/projects/kindlm && npx vitest run packages/cli/src/utils/dry-run.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "estimatedCostUsd" packages/cli/src/utils/dry-run.ts shows usage of the field
    - grep -n "totalEstimatedCostUsd" packages/cli/src/utils/dry-run.ts shows usage
    - grep -n "Estimated cost" packages/cli/src/utils/dry-run.ts shows the summary line
    - All dry-run.test.ts tests pass
  </acceptance_criteria>
  <done>
    formatTestPlan renders per-entry cost and total estimated cost; all tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Type-check and full phase verification</name>
  <files></files>
  <read_first>
    packages/core/src/engine/test-plan.ts
    packages/cli/src/utils/dry-run.ts
  </read_first>
  <action>
    Run the full workspace type-check and test suite to confirm no regressions.
    Also verify the dry-run output structure satisfies all five requirements by
    reviewing the code:
    - DRY-01: --dry-run path exits before runTests() — confirmed in test.ts line 67-93
    - DRY-02: entry line shows testName + modelId + repeat — confirmed in dry-run.ts
    - DRY-03: entry line shows assertionTypes — confirmed in dry-run.ts assertionLabel
    - DRY-04: entry line shows estimatedCostUsd, summary shows totalEstimatedCostUsd — Task 1
    - DRY-05: summary shows totalExecutionUnits — confirmed in dry-run.ts existing line

    Fix any TypeScript errors found. Do not add new features.
  </action>
  <verify>
    <automated>cd /Users/petr/projects/kindlm && npm run typecheck 2>&1 | tail -20 && npm run test 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - npm run typecheck exits 0 (no TS errors)
    - npm run test exits 0 (no test failures)
    - grep -n "totalEstimatedCostUsd" packages/core/src/engine/test-plan.ts shows field in interface
    - grep -n "Estimated cost" packages/cli/src/utils/dry-run.ts shows the output line
  </acceptance_criteria>
  <done>
    All workspace tests pass; TypeScript clean; all five DRY requirements verified in code
  </done>
</task>

</tasks>

<verification>
cd /Users/petr/projects/kindlm && npm run typecheck && npm run test
</verification>

<success_criteria>
- formatTestPlan shows per-entry cost (~$0.000010 or ~$?) on each active test line
- formatTestPlan shows "Estimated cost: ~$X.XXXXXX" or "Estimated cost: unknown" in summary
- Full test suite passes with zero failures
- TypeScript strict-mode clean across workspace
- All five requirements (DRY-01 through DRY-05) implemented and verifiable in code
</success_criteria>

<output>
After completion, create .planning/phases/11-dry-run/11-02-SUMMARY.md
</output>
