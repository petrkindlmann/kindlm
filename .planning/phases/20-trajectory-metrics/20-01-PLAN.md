---
phase: 20-trajectory-metrics
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/core/src/assertions/trajectory.ts
  - packages/core/src/assertions/trajectory.test.ts
autonomous: true
requirements: [TRAJ-01, TRAJ-02, TRAJ-03, TRAJ-04]
must_haves:
  truths:
    - "trajectory_precision = |pred ∩ ref| / |pred| computed via multiset intersection (TRAJ-01)"
    - "trajectory_recall = |pred ∩ ref| / |ref| computed via multiset intersection (TRAJ-02)"
    - "trajectory_exact_match returns binary 0/1 on identical tools in identical order (TRAJ-03)"
    - "ordered:false switches precision/recall/exact_match to any-order (multiset) matching (TRAJ-04)"
    - "Duplicate tool calls are scored as a multiset, not a set (research pitfall #1)"
    - "Empty predicted sequence yields precision 0 (not NaN, not throw) with a clear failure message"
    - "Argument key ordering is canonicalized so {a,b} and {b,a} fingerprint identically"
    - "matchArgs:false matches on tool name only, ignoring arguments"
  artifacts:
    - path: "packages/core/src/assertions/trajectory.ts"
      provides: "createTrajectoryAssertion factory + pure fingerprint/multiset/exact-match math"
      exports: ["createTrajectoryAssertion", "TrajectoryConfig", "TrajectoryAction"]
      min_lines: 80
    - path: "packages/core/src/assertions/trajectory.test.ts"
      provides: "Golden-value + edge-case unit tests for all 4 TRAJ requirements"
      min_lines: 120
  key_links:
    - from: "packages/core/src/assertions/trajectory.ts"
      to: "packages/core/src/assertions/interface.ts"
      via: "implements Assertion, returns AssertionResult[]"
      pattern: "Assertion|AssertionResult|AssertionContext"
---

<objective>
Build the pure trajectory-metric foundation: a single `createTrajectoryAssertion` factory
in a new self-contained `trajectory.ts` module, plus colocated unit tests written test-first
(RED → GREEN). This plan owns ALL the math — canonical action fingerprint, multiset
intersection, sequence equality, and the precision/recall/exact_match result emission — and
none of the schema/registry wiring (that is Plan 20-02).

Implements TRAJ-01 (precision), TRAJ-02 (recall), TRAJ-03 (exact_match), TRAJ-04 (ordered toggle).

Purpose: trajectory scoring is crisp mathematical I/O — TDD fits perfectly and isolates the
formulas from the (shared-file) schema/registry surface so Plan 20-02 can wire cleanly.
Output: `trajectory.ts` (factory + math) and `trajectory.test.ts` (golden + edge tests), both new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-trajectory-metrics/20-RESEARCH.md

# Note for execute-time: Phase 20 builds ON Phase 19's branch (depends_on: Phase 19),
# NOT on main. Confirm the working branch is the Phase 19 branch before starting.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing trajectory unit tests (RED)</name>
  <files>packages/core/src/assertions/trajectory.test.ts</files>
  <read_first>
    - .planning/phases/20-trajectory-metrics/20-RESEARCH.md — sections "Code Examples (Golden Test Trajectories)" (the full `trajectory.test.ts` skeleton), "Testing Strategy" (the 12 required cases), and "Common Pitfalls" (1–5)
    - packages/core/src/assertions/interface.ts — AssertionContext (note: `toolCalls`, `outputText`, `configDir` are required fields), AssertionResult, Assertion
    - packages/core/src/types/provider.ts — ProviderToolCall shape `{ id, name, arguments, index }`
    - packages/core/src/assertions/tool-calls.test.ts — for the project's Vitest style + how `context.toolCalls` is synthesized in tests
  </read_first>
  <behavior>
    - Precision golden value: reference=[lookup_order{id:1}, issue_refund{id:1,amount:50}], predicted=[lookup_order{id:1}] → precision 1.0, passed at minScore 1.0
    - Precision penalty: predicted has an extra unmatched tool (send_spam) → precision 0.5, failed at minScore 1.0
    - Recall golden value: predicted misses one reference step → recall 0.5
    - Recall happy path: predicted covers all reference steps → recall 1.0
    - Exact match ordered=true: identical ordered sequence → score 1; reversed order → score 0
    - Ordered toggle (TRAJ-04): exactMatch + ordered=false on a permuted sequence → score 1
    - Multiset/duplicate (pitfall #1): reference=[search,search], predicted=[search,search,search], matchArgs=false → precision 2/3 (toBeCloseTo), recall 1.0
    - Canonical arg order: reference f{a:1,b:2} vs predicted f{b:2,a:1}, exactMatch ordered → passed (toBe true)
    - matchArgs=false ignores args: reference send_email{to:real} vs predicted send_email{to:different} → exactMatch passed
    - Empty predicted: predicted=[] → precision score 0 AND failureMessage contains "pred=0"
    - All three metrics enabled at once → evaluate() returns exactly 3 results (one each: trajectory_precision, trajectory_recall, trajectory_exact_match)
    - Prototype-safety: a reference/predicted arg containing a `__proto__` key does not pollute Object.prototype and produces a deterministic fingerprint (security: prototype pollution mitigation)
  </behavior>
  <action>
    Create `trajectory.test.ts` colocated next to (not-yet-created) `trajectory.ts`. Import
    `createTrajectoryAssertion` from `./trajectory.js` (the `.js` extension is mandatory — ESM).
    Add a `makeContext(calls)` helper that maps `{name, arguments}[]` to `ProviderToolCall[]`
    (set `id`, `index`, and include required `AssertionContext` fields `outputText: ""` and
    `configDir: "/tmp"`). Write the cases listed in <behavior>, sourcing exact golden numbers
    from RESEARCH.md "Code Examples". Name tests so they contain the substrings the research
    test-map greps for: "precision", "recall", "exact_match", "ordered". Run the test file and
    confirm it FAILS to import / compile (RED) before any implementation exists. Do NOT write the
    implementation in this task. No fenced code in the plan — copy structure from RESEARCH.md.
  </action>
  <verify>
    <automated>cd packages/core && npx vitest run src/assertions/trajectory.test.ts 2>&1 | grep -Eq 'Cannot find module|failed|FAIL' && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - `trajectory.test.ts` exists with ≥12 `it(...)` cases covering all behaviors above.
    - Running the file fails (RED) because `./trajectory.js` does not yet export `createTrajectoryAssertion`.
    - Test names include the substrings "precision", "recall", "exact_match", and "ordered".
    - Duplicate-call multiset test asserts precision `toBeCloseTo(2/3)` and recall `toBe(1)`.
    - Empty-predicted test asserts score 0 and `failureMessage` contains `"pred=0"`.
  </acceptance_criteria>
  <done>trajectory.test.ts present, fails to run because implementation is absent (RED state confirmed).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement createTrajectoryAssertion to pass the tests (GREEN)</name>
  <files>packages/core/src/assertions/trajectory.ts</files>
  <read_first>
    - packages/core/src/assertions/trajectory.test.ts — the tests written in Task 1 (the spec)
    - .planning/phases/20-trajectory-metrics/20-RESEARCH.md — sections "TypeScript Formula Implementations" (the reference `trajectory.ts`), "Pattern 1: Canonical Action Fingerprint", "Pattern 2: Multiset Intersection", "Pattern 3: One Factory, Multiple Results", "Anti-Patterns to Avoid", and "Security Domain" (prototype-pollution + JSON.stringify safety)
    - packages/core/src/assertions/tool-calls.ts — analog factory: how an Assertion is shaped, `Promise.resolve(results)` return style, AssertionResult field usage
    - packages/core/src/assertions/interface.ts — Assertion / AssertionResult / AssertionContext contracts (use `import type`)
    - packages/core/src/types/provider.ts — ProviderToolCall (`import type`)
  </read_first>
  <action>
    Create `trajectory.ts` following RESEARCH.md "TypeScript Formula Implementations" precisely.
    Export interfaces `TrajectoryAction` (`tool: string; args: Record<string, unknown>`) and
    `TrajectoryConfig` (`reference`, optional `precision?:{minScore}`, optional `recall?:{minScore}`,
    `exactMatch: boolean`, `ordered: boolean`, `matchArgs: boolean`). Implement private helpers:
    `canonicalizeArgs` (recursive key sort via `Object.keys(o).sort()` then `JSON.stringify` —
    prototype-safe, Workers-compatible, no Node imports), `fingerprint(call, matchArgs)`
    (returns `name` when matchArgs is false, else `` `${name}::${canonicalizeArgs(args)}` ``),
    `multisetIntersectionSize(predicted, reference)` (Map<string,number> count-decrement, O(n+m)),
    and `sequencesEqual(a, b)`. Export `createTrajectoryAssertion(config): Assertion` with
    `type: "trajectory"` whose `evaluate(context)` maps `context.toolCalls` and `config.reference`
    to fingerprint arrays once, then: for `ordered:false` sort both arrays before exact comparison
    (precision/recall use multiset intersection which is order-insensitive natively); push a result
    with `assertionType: "trajectory_precision"` (guard `pred.length === 0 ? 0` → failureCode
    `TRAJECTORY_PRECISION_LOW`), `"trajectory_recall"` (failureCode `TRAJECTORY_RECALL_LOW`), and
    `"trajectory_exact_match"` (failureCode `TRAJECTORY_EXACT_MISMATCH`), each only when its config
    flag is set. Include `metadata.predicted`, `metadata.reference`, `metadata.ordered`,
    `metadata.matchArgs`, and per Open Question #3 also `metadata.predictedRaw` (the raw
    `context.toolCalls`) for downstream Phase 24 diffing. Return `Promise.resolve(results)`. NEVER
    throw. Use `import type` for all type-only imports and `.js` extensions on relative imports.
    The three failureCode string literals MUST match the FailureCode union additions made in
    Plan 20-02 — if `interface.ts` does not yet contain them, this file will not typecheck until
    20-02 lands; that ordering is intentional (20-02 is wave 2). For an isolated GREEN run, the
    string literals are still emitted correctly regardless of the union.
  </action>
  <verify>
    <automated>cd packages/core && npx vitest run src/assertions/trajectory.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All `trajectory.test.ts` cases pass (GREEN).
    - `createTrajectoryAssertion`, `TrajectoryConfig`, `TrajectoryAction` are exported from `trajectory.ts`.
    - No `fs`/`fetch`/`console.*` and no Node built-ins (zero-I/O core rule); only `JSON`/`Object`/`Map`.
    - No ES6 `class`; factory function only. No `any`. All relative imports end in `.js`. Type-only imports use `import type`.
    - Multiset duplicate test passes (precision 2/3, recall 1) — research pitfall #1 covered.
    - `cd packages/core && npx eslint src/assertions/trajectory.ts src/assertions/trajectory.test.ts --quiet` exits 0.
  </acceptance_criteria>
  <done>trajectory.ts implements all four TRAJ metrics; trajectory.test.ts fully green; eslint clean on both new files.</done>
</task>

</tasks>

<verification>
- `cd packages/core && npx vitest run src/assertions/trajectory.test.ts` — all green.
- `cd packages/core && npx eslint src/assertions/trajectory.ts src/assertions/trajectory.test.ts --quiet` — exit 0.
- Module is fully self-contained: it does NOT import from registry.ts or schema.ts, so it can be
  tested in isolation before Plan 20-02 wires it in.
</verification>

<success_criteria>
- TRAJ-01 precision, TRAJ-02 recall, TRAJ-03 exact_match, TRAJ-04 ordered toggle all implemented and unit-proven.
- Multiset duplicate-call correctness has an explicit passing test (research pitfall #1).
- Empty-predicted, canonical-arg-order, matchArgs:false, and prototype-pollution edge cases all tested.
</success_criteria>

<output>
Create `.planning/phases/20-trajectory-metrics/20-01-SUMMARY.md` when done.
</output>
