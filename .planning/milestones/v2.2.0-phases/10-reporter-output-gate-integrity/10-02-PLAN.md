---
phase: 10-reporter-output-gate-integrity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/engine/gate.ts
  - packages/core/src/engine/gate.test.ts
  - packages/core/src/reporters/pretty.ts
autonomous: true
requirements: [GATE-01, GATE-02, GATE-03]

must_haves:
  truths:
    - "When judgeAvgMin gate has zero judge assertions, the Quality Gates output shows a ⚠ icon and message containing 'no judge assertions found — gate trivially passed'"
    - "When driftScoreMax gate has zero drift assertions, the Quality Gates output shows a ⚠ icon and message containing 'no drift assertions found — gate trivially passed'"
    - "When deterministicPassRate gate has zero deterministic assertions, the Quality Gates output shows a ⚠ icon and message containing 'no deterministic assertions found — gate trivially passed'"
    - "When probabilisticPassRate gate has zero probabilistic assertions, the Quality Gates output shows a ⚠ icon and message containing 'no probabilistic assertions found — gate trivially passed'"
    - "All empty-data gates still pass (not fail) — D-08"
  artifacts:
    - path: "packages/core/src/engine/gate.ts"
      provides: "Updated gate evaluation with emptyData flag on GateResult"
      contains: "emptyData"
    - path: "packages/core/src/reporters/pretty.ts"
      provides: "Warning icon rendering for empty-data gates"
      contains: "emptyData"
    - path: "packages/core/src/engine/gate.test.ts"
      provides: "Tests for all four empty-data gate warning scenarios"
      contains: "trivially passed"
  key_links:
    - from: "packages/core/src/engine/gate.ts"
      to: "packages/core/src/reporters/pretty.ts"
      via: "GateResult.emptyData boolean flag"
      pattern: "emptyData"
---

<objective>
Add an `emptyData` flag to `GateResult` for the four optional gates that trivially pass when no matching assertions exist. The pretty reporter renders the ⚠ icon for flagged gates and appends the warning text to the existing message — gate still passes, users now know it was vacuous.

Purpose: A gate that "passes" because there's nothing to evaluate gives a false sense of security. Developers need to know their gate configuration isn't actually exercising anything.
Output: Updated `GateResult` type, updated `evaluateGates()` for 4 gates, updated pretty reporter gate rendering; passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-reporter-output-gate-integrity/10-CONTEXT.md

<interfaces>
<!-- Key contracts. Do NOT explore codebase — use these directly. -->

From packages/core/src/engine/gate.ts — current GateResult (lines 5-11):
```typescript
export interface GateResult {
  gateName: string;
  passed: boolean;
  actual: number;
  threshold: number;
  message: string;
}
```

From packages/core/src/engine/gate.ts — empty-data defaults at:
- Line 63: `judgeAvgMin` fallback: `const judgeAvg = judgeScores.length > 0 ? judgeScores.reduce(...) / judgeScores.length : 1;`
- Line 84: `driftScoreMax` fallback: `const maxDrift = driftValues.length > 0 ? Math.max(...driftValues) : 0;`
- Line 211: `computeCategoryPassRate` fallback: `return total > 0 ? passed / total : 1;`

From packages/core/src/reporters/pretty.ts — current gate rendering (lines 59-66):
```typescript
if (gateEvaluation.gates.length > 0) {
  lines.push(c.bold("  Quality Gates"));
  for (const gate of gateEvaluation.gates) {
    const icon = gate.passed ? c.green("✓") : c.red("✗");
    lines.push(`    ${icon} ${gate.message}`);
  }
  lines.push("");
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add emptyData flag to GateResult and set it in evaluateGates()</name>
  <files>packages/core/src/engine/gate.ts, packages/core/src/engine/gate.test.ts</files>
  <read_first>
    - packages/core/src/engine/gate.ts (full file — understand all 10 gates)
    - packages/core/src/engine/gate.test.ts (existing tests — don't break them)
  </read_first>
  <behavior>
    - Test 1: judgeAvgMin gate with zero judge scores → GateResult.emptyData === true, passed === true, message contains "no judge assertions found — gate trivially passed"
    - Test 2: judgeAvgMin gate with at least one judge score → GateResult.emptyData is undefined or false
    - Test 3: driftScoreMax gate with zero drift scores → GateResult.emptyData === true, passed === true, message contains "no drift assertions found — gate trivially passed"
    - Test 4: driftScoreMax gate with at least one drift score → no emptyData flag
    - Test 5: deterministicPassRate gate with zero deterministic assertions → GateResult.emptyData === true, message contains "no deterministic assertions found — gate trivially passed"
    - Test 6: probabilisticPassRate gate with zero probabilistic assertions → GateResult.emptyData === true, message contains "no probabilistic assertions found — gate trivially passed"
    - Test 7: passRateMin gate (always has data or meaningful zero) → never sets emptyData
  </behavior>
  <action>
**Decision traceability:** D-05, D-06, D-07, D-08 from CONTEXT.md.

1. Add `emptyData?: true` to `GateResult` interface (optional field, only set when truly empty):
   ```typescript
   export interface GateResult {
     gateName: string;
     passed: boolean;
     actual: number;
     threshold: number;
     message: string;
     emptyData?: true;
   }
   ```

2. In `evaluateGates()`, update the **four affected gates** to detect empty data and set the flag + append warning to message:

   **judgeAvgMin** (around line 59-75): Detect `judgeScores.length === 0` before the fallback.
   When empty, set `emptyData: true` and append ` (no judge assertions found — gate trivially passed)` to message.

   **driftScoreMax** (around line 80-95): Detect `driftScores.length === 0` before the fallback.
   When empty, set `emptyData: true` and append ` (no drift assertions found — gate trivially passed)` to message.

   **deterministicPassRate** (around line 159-170): `computeCategoryPassRate` returns 1 when total === 0 — need to detect this case. Either:
   - Change `computeCategoryPassRate` to return `{ rate: number; empty: boolean }`, OR
   - Add a separate `countCategoryAssertions()` helper and check it before calling compute
   Use whichever approach is cleaner. When zero deterministic assertions, set `emptyData: true` and append ` (no deterministic assertions found — gate trivially passed)`.

   **probabilisticPassRate** (around line 174-186): Same pattern as deterministicPassRate. Append ` (no probabilistic assertions found — gate trivially passed)`.

3. The six other gates (passRateMin, schemaFailuresMax, piiFailuresMax, keywordFailuresMax, costMaxUsd, latencyMaxMs) are NOT touched — they either always have data or have meaningful zero semantics.

Write tests FIRST (RED), implement (GREEN).
  </action>
  <verify>
    <automated>cd /Users/petr/projects/kindlm && npx vitest run packages/core/src/engine/gate.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "emptyData" packages/core/src/engine/gate.ts` returns at least 4 matches (one per affected gate)
    - `grep -n "trivially passed" packages/core/src/engine/gate.ts` returns at least 4 matches
    - `grep -n "emptyData" packages/core/src/engine/gate.test.ts` returns test cases
    - All gate tests pass (exit code 0)
    - `npx tsc --noEmit` in packages/core passes
  </acceptance_criteria>
  <done>GateResult has optional emptyData flag; judgeAvgMin, driftScoreMax, deterministicPassRate, probabilisticPassRate set it when their input collections are empty; all tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Render ⚠ warning icon for empty-data gates in pretty reporter</name>
  <files>packages/core/src/reporters/pretty.ts</files>
  <read_first>
    - packages/core/src/reporters/pretty.ts (full file — see current gate rendering at lines 59-66)
    - packages/core/src/engine/gate.ts (confirm emptyData field was added in Task 1)
  </read_first>
  <action>
**Decision traceability:** D-05 from CONTEXT.md.

Update the gate rendering loop in `pretty.ts` (lines 59-66) to use `c.yellow("⚠")` when `gate.emptyData === true`:

```typescript
for (const gate of gateEvaluation.gates) {
  const icon = gate.emptyData
    ? c.yellow("⚠")
    : gate.passed
      ? c.green("✓")
      : c.red("✗");
  lines.push(`    ${icon} ${gate.message}`);
}
```

The message already contains the warning text (added in Task 1), so no additional text change is needed here — the icon change is the only reporter modification.

No new test file needed for this task — the gate.test.ts tests in Task 1 verify the message content, and existing pretty.test.ts snapshot tests (if any) will catch icon regressions. Add one targeted test to pretty.test.ts if no existing test covers gate rendering with the emptyData flag.
  </action>
  <verify>
    <automated>cd /Users/petr/projects/kindlm && npx vitest run packages/core/src/reporters/ 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "emptyData" packages/core/src/reporters/pretty.ts` returns the conditional icon check
    - `grep -n 'yellow.*⚠\|⚠.*yellow' packages/core/src/reporters/pretty.ts` returns at least one match
    - All reporter tests pass
    - `npx tsc --noEmit` passes — `gate.emptyData` is typed correctly (TypeScript resolves optional field)
  </acceptance_criteria>
  <done>Pretty reporter shows ⚠ (yellow) icon for gates with emptyData:true; ✓/✗ icons unchanged for all other gates.</done>
</task>

</tasks>

<verification>
1. `cd /Users/petr/projects/kindlm && npx vitest run packages/core/src/engine/gate.test.ts` — all pass
2. `cd /Users/petr/projects/kindlm && npx vitest run packages/core/src/reporters/` — all pass
3. `cd /Users/petr/projects/kindlm && npx tsc --noEmit` — no type errors
4. Construct a GateEvaluation with one emptyData gate — pretty output contains "⚠" and "trivially passed"
5. Construct a normal passing gate — still renders "✓"
6. Construct a failing gate — still renders "✗"
</verification>

<success_criteria>
- GATE-01: judgeAvgMin with zero judge assertions → ⚠ warning in output
- GATE-02: driftScoreMax with zero drift assertions → ⚠ warning in output
- GATE-03: deterministicPassRate/probabilisticPassRate with zero assertions → ⚠ warning in output
- All empty-data gates still pass (not fail) per D-08
- Zero regressions in existing gate or reporter tests
- TypeScript strict mode passes
</success_criteria>

<output>
After completion, create `.planning/phases/10-reporter-output-gate-integrity/10-02-SUMMARY.md`
</output>
