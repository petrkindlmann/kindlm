---
phase: 03-feature-flags
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/cli/src/utils/features.ts
  - packages/cli/src/utils/run-tests.ts
autonomous: true
requirements:
  - FF-01
  - FF-02
  - FF-03

must_haves:
  truths:
    - "isEnabled('runArtifacts') returns false when .kindlm/config.json is absent"
    - "isEnabled('betaJudge') returns true when config.json sets { features: { betaJudge: true } }"
    - "runTests passes featureFlags into RunTestsOptions and conditionally skips artifact writing"
    - "packages/core never imports features.ts"
  artifacts:
    - path: "packages/cli/src/utils/features.ts"
      provides: "loadFeatureFlags(), isEnabled() — CLI-only feature flag reader"
      exports: ["loadFeatureFlags", "isEnabled", "FeatureFlags"]
    - path: "packages/cli/src/utils/run-tests.ts"
      provides: "wires feature flags into RunTestsOptions, gates runArtifacts"
      contains: "featureFlags"
  key_links:
    - from: "packages/cli/src/utils/run-tests.ts"
      to: "packages/cli/src/utils/features.ts"
      via: "import { loadFeatureFlags } at top of runTestsInner"
      pattern: "loadFeatureFlags"
---

<objective>
Add a lightweight feature-flag system to the CLI layer: read `.kindlm/config.json` at startup, expose `isEnabled(flag)`, and gate the three initial flags (`betaJudge`, `costGating`, `runArtifacts`) in `run-tests.ts`.

Purpose: Let users opt into experimental behaviour without changing `kindlm.yaml`. Keeps core zero-I/O and flags CLI-only.
Output: `features.ts` (new), updated `run-tests.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/cli/src/utils/run-tests.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create features.ts — feature flag reader</name>
  <files>packages/cli/src/utils/features.ts, packages/cli/src/utils/features.test.ts</files>
  <behavior>
    - loadFeatureFlags(cwd?: string): reads `{cwd}/.kindlm/config.json` synchronously; returns FeatureFlags object; returns all-false defaults when file is absent, unreadable, or JSON is malformed (never throws)
    - isEnabled(flags: FeatureFlags, flag: keyof FeatureFlags): returns boolean; defaults to false if key missing
    - FeatureFlags type: `{ betaJudge: boolean; costGating: boolean; runArtifacts: boolean }`
    - test: absent file → all flags false
    - test: malformed JSON → all flags false (no throw)
    - test: { features: { betaJudge: true } } → betaJudge=true, others false
    - test: unknown keys in features are ignored (no type error, no crash)
  </behavior>
  <action>
Create `packages/cli/src/utils/features.ts` using only `node:fs` and `node:path`. No runtime dependencies.

```
// When to add a feature flag vs. just shipping:
//   ADD a flag when: the feature is experimental, opt-in-only, or may break existing runs.
//   SHIP directly when: the feature is backward-compatible and safe for all users.
//   REMOVE a flag when: the feature graduates to default-on after one minor release cycle.
//
// Config file: .kindlm/config.json (project-level, gitignore this file)
// Schema: { "features": { "flagName": boolean } }
// All flags default to false — absent file = all disabled.
```

Implementation shape:
- `export type FeatureFlags = { betaJudge: boolean; costGating: boolean; runArtifacts: boolean }`
- `export function loadFeatureFlags(cwd = process.cwd()): FeatureFlags`
  - resolve path: `path.join(cwd, ".kindlm", "config.json")`
  - wrap `readFileSync` + `JSON.parse` in try/catch; on any error return defaults
  - after parse, extract `parsed?.features` — iterate known keys, set to `!!value`
  - return merged object (unknown keys in JSON are silently ignored)
- `export function isEnabled(flags: FeatureFlags, flag: keyof FeatureFlags): boolean`
  - return `flags[flag] ?? false`

Use `import type` for any type-only imports (verbatimModuleSyntax). All relative imports use `.js` extension. No `any` — use `unknown` for parsed JSON.
  </action>
  <verify>
    <automated>cd /Users/petr/projects/kindlm && npx vitest run packages/cli/src/utils/features.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All 4 behaviour tests pass. tsc --noEmit reports no errors on the file.</done>
</task>

<task type="auto">
  <name>Task 2: Wire feature flags into run-tests.ts</name>
  <files>packages/cli/src/utils/run-tests.ts</files>
  <action>
Extend `RunTestsOptions` with one optional field:

```typescript
featureFlags?: FeatureFlags;
```

At the top of `runTestsInner`, before step 1 (read config), add:

```typescript
const featureFlags = options.featureFlags ?? loadFeatureFlags(process.cwd());
```

Import `loadFeatureFlags` and `FeatureFlags` from `./features.js`.

Gate artifact writing: after `const runResult = await runner.run();` and before `spinner.stop()`, add:

```typescript
// runArtifacts flag: only write run artifacts when explicitly opted in.
// Phase 02 wires the actual writer here once that phase ships.
if (isEnabled(featureFlags, "runArtifacts")) {
  // artifact writer will be injected here in phase 02
}
```

Also pass `featureFlags` through to the returned `RunTestsResult` so callers (e.g. `test.ts` command) can inspect flags without re-loading:

```typescript
export interface RunTestsResult {
  config: KindLMConfig;
  runnerResult: RunnerResult;
  configDir: string;
  yamlContent: string;
  featureFlags: FeatureFlags;  // add this
}
```

Return `featureFlags` in the result object at the end of `runTestsInner`.

No behaviour change to existing tests — `featureFlags` is optional on options, and `loadFeatureFlags` returns all-false defaults when `.kindlm/config.json` is absent.

`betaJudge` and `costGating` flags are loaded but not yet consumed — they are reserved for the assertion layer and gate enforcement respectively. Add a comment above each noting the future consumer.
  </action>
  <verify>
    <automated>cd /Users/petr/projects/kindlm && npm run typecheck 2>&1 | tail -30</automated>
  </verify>
  <done>typecheck passes with zero errors. RunTestsOptions and RunTestsResult compile cleanly with the new fields. Existing callers (test.ts, trace.ts, upload.ts) are unaffected because featureFlags is optional.</done>
</task>

</tasks>

<verification>
cd /Users/petr/projects/kindlm && npx vitest run packages/cli/src/utils/features.test.ts && npm run typecheck 2>&1 | tail -10
</verification>

<success_criteria>
- features.ts exists and exports FeatureFlags, loadFeatureFlags, isEnabled
- All 4 unit tests in features.test.ts pass
- npm run typecheck reports 0 errors
- packages/core has no import of features.ts (grep confirms)
- .kindlm/config.json absent → all flags return false (tested)
</success_criteria>

<output>
After completion, create `.planning/phases/03-feature-flags-via-config-boolean-flags-in-kindlm-config-json-isenabled-helper-gate-new-assertion-types-behind-flags/03-01-SUMMARY.md`
</output>
