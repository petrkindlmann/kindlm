---
phase: 01-deploy-everything
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/providers/http.test.ts
  - packages/cloud/src/db/queries/orgs.ts
  - packages/cloud/src/saml/helpers.ts
  - packages/cloud/src/saml/xml-parser.ts
autonomous: true
requirements:
  - OPS-01
  - OPS-04

must_haves:
  truths:
    - "npx turbo run typecheck --filter=@kindlm/core --filter=@kindlm/cli exits 0"
    - "npx turbo run lint --filter=@kindlm/cloud exits 0"
    - "Migration files 0001-0013 exist sequentially with no duplicates"
  artifacts:
    - path: "packages/core/src/providers/http.test.ts"
      provides: "Type-safe test assertions for HTTP provider"
    - path: "packages/cloud/src/db/queries/orgs.ts"
      provides: "Clean import without unused User type"
    - path: "packages/cloud/src/saml/helpers.ts"
      provides: "No unused vars, no non-null assertions"
    - path: "packages/cloud/src/saml/xml-parser.ts"
      provides: "No unused deepFindDsTag function"
  key_links:
    - from: "packages/core/src/providers/http.test.ts"
      to: "typecheck"
      via: "tsc --noEmit"
      pattern: "fetchCall!"
    - from: "packages/cloud/src/saml/helpers.ts"
      to: "lint"
      via: "eslint"
      pattern: "ieeeP1363ToDer|non-null"
---

<objective>
Fix all CI blockers: typecheck errors in @kindlm/core and lint errors in @kindlm/cloud. Also verify OPS-01 (migration renumbering) is already resolved.

Purpose: CI must pass green before pushing code and deploying. The typecheck fails on `http.test.ts` (9 errors: 'possibly undefined' on fetchCall variable) and lint fails on 3 cloud files (3 unused vars + 1 non-null assertion).

Output: Clean CI — typecheck and lint pass on all packages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix typecheck errors in core/providers/http.test.ts</name>
  <files>packages/core/src/providers/http.test.ts</files>
  <read_first>
    - packages/core/src/providers/http.test.ts (full file — need to see all 9 error locations)
  </read_first>
  <action>
Fix 9 TypeScript strict-mode errors in `packages/core/src/providers/http.test.ts`. All errors are "possibly undefined" on the `fetchCall` variable pattern.

The variable is assigned like:
```typescript
const fetchCall = (mockClient.fetch as ReturnType<typeof vi.fn>).mock.calls[0];
```

This returns `T | undefined` because `.mock.calls[0]` might not exist. Fix by adding a non-null assertion after the assignment with a guard, or by using `!` on the assignment since the test would already fail if no call was made. The idiomatic Vitest approach for test files:

**Option A (preferred — explicit guard):** After each `fetchCall` assignment, add:
```typescript
const fetchCall = (mockClient.fetch as ReturnType<typeof vi.fn>).mock.calls[0];
expect(fetchCall).toBeDefined();
```
Then use `fetchCall!` or cast to the known type for subsequent property accesses.

**Option B (simpler):** Add `!` non-null assertion to the assignment:
```typescript
const fetchCall = (mockClient.fetch as ReturnType<typeof vi.fn>).mock.calls[0]!;
```

Use Option B since this is a test file where the calls are guaranteed by the preceding test setup, and non-null assertions are acceptable in test code. Apply to all 9 error locations at these lines (approximate — verify by reading file):
- Line 228: `const fetchCall = ...mock.calls[0]` — add `!` after `[0]`
- Line 256: same pattern
- Line 284: same pattern
- Line 322: similar pattern with `.mock.calls[0]` — add `!` after `[0]`
- Line 323: same object access — will be fixed by fixing line 322
- Line 354: same pattern
- Line 355: same object access
- Line 485: same pattern

After fixing, run: `cd packages/core && npx tsc --noEmit` to verify 0 errors.
  </action>
  <verify>
    <automated>cd /Users/petr/projects/kindlm && npx turbo run typecheck --filter=@kindlm/core --filter=@kindlm/cli 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx turbo run typecheck --filter=@kindlm/core --filter=@kindlm/cli` exits with code 0
    - No lines in http.test.ts contain `.mock.calls[0];` without a `!` before the semicolon (for the fetchCall pattern)
    - grep for "TS18048\|TS2532" in typecheck output returns empty
  </acceptance_criteria>
  <done>TypeScript typecheck passes for @kindlm/core and @kindlm/cli with zero errors</done>
</task>

<task type="auto">
  <name>Task 2: Fix lint errors in cloud package (3 files)</name>
  <files>packages/cloud/src/db/queries/orgs.ts, packages/cloud/src/saml/helpers.ts, packages/cloud/src/saml/xml-parser.ts</files>
  <read_first>
    - packages/cloud/src/db/queries/orgs.ts (lines 1-10 — see the unused User import)
    - packages/cloud/src/saml/helpers.ts (lines 195-215 — see ieeeP1363ToDer and non-null assertion)
    - packages/cloud/src/saml/xml-parser.ts (lines 85-95 — see deepFindDsTag)
  </read_first>
  <action>
Fix 4 ESLint errors across 3 files in `packages/cloud/`:

**File 1: `src/db/queries/orgs.ts` line 6**
- Error: `'User' is defined but never used`
- Fix: Remove `User` from the import statement on line 6. It is imported as `import type { ..., User, ... } from "../../types.js"`. Remove `User` from the destructured import list. If `User` is the only import, change to `import type { ... }` without it.

**File 2: `src/saml/helpers.ts` line 198**
- Error: `'ieeeP1363ToDer' is defined but never used`
- Fix: If the function `ieeeP1363ToDer` (line 198) is not called anywhere in the file, prefix it with underscore: rename to `_ieeeP1363ToDer`. OR if it was meant to be exported, add `export`. Check if it's used elsewhere in the codebase first with grep. If truly unused, prefix with `_`.

**File 2: `src/saml/helpers.ts` line 209**
- Error: `Forbidden non-null assertion`
- Fix: Replace the non-null assertion `!` on line 209 with a proper null check. Read the line to see the exact expression, then add a guard: `if (value === null || value === undefined) throw new Error('...')` before the usage, then remove the `!`.

**File 3: `src/saml/xml-parser.ts` line 91**
- Error: `'deepFindDsTag' is defined but never used`
- Fix: Same approach as ieeeP1363ToDer — prefix with `_` if unused, or `export` if intended for external use. Check usage first.

After fixing all 4 errors, run: `cd packages/cloud && npx eslint src/` to verify 0 errors.
  </action>
  <verify>
    <automated>cd /Users/petr/projects/kindlm && npx turbo run lint --filter=@kindlm/cloud 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx turbo run lint --filter=@kindlm/cloud` exits with code 0
    - grep `"'User' is defined but never used"` in lint output returns empty
    - grep `"'ieeeP1363ToDer' is defined but never used"` in lint output returns empty
    - grep `"Forbidden non-null assertion"` in lint output returns empty
    - grep `"'deepFindDsTag' is defined but never used"` in lint output returns empty
  </acceptance_criteria>
  <done>ESLint passes for @kindlm/cloud with zero errors. All 4 lint issues resolved.</done>
</task>

<task type="auto">
  <name>Task 3: Verify OPS-01 migration state (already fixed)</name>
  <files></files>
  <read_first>
    - packages/cloud/migrations/ (list directory to verify sequential numbering)
  </read_first>
  <action>
Verify that OPS-01 (duplicate migration 0011 renumbering) is already resolved per commit cd7f3cc.

1. List all migration files in `packages/cloud/migrations/` and confirm:
   - Files exist: 0001 through 0013
   - No duplicate numbers (especially no two 0011 files)
   - 0013_index_audit.sql exists (the renamed file)
   - 0011_audit_token_enhancements.sql exists (the original that kept its number)
   - 0012_compliance_signing.sql exists

2. If all checks pass, OPS-01 is confirmed done. No code changes needed.
3. If any check fails, flag it as a blocker for the executor to investigate.
  </action>
  <verify>
    <automated>ls /Users/petr/projects/kindlm/packages/cloud/migrations/ | sort | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `ls packages/cloud/migrations/` shows exactly 13 files numbered 0001 through 0013
    - No file matching `*0011*` appears more than once
    - File `0013_index_audit.sql` exists
  </acceptance_criteria>
  <done>OPS-01 confirmed resolved. Migrations numbered sequentially 0001-0013 with no duplicates.</done>
</task>

</tasks>

<verification>
1. `npx turbo run typecheck --filter=@kindlm/core --filter=@kindlm/cli` exits 0
2. `npx turbo run lint --filter=@kindlm/cloud` exits 0
3. `ls packages/cloud/migrations/ | wc -l` equals 13
4. Full CI check: `npx turbo run build typecheck lint test` exits 0 (optional — may have other pre-existing issues)
</verification>

<success_criteria>
- CI-blocking typecheck errors in http.test.ts are fixed (0 TS errors)
- CI-blocking lint errors in cloud package are fixed (0 ESLint errors)
- Migration numbering verified as sequential 0001-0013
- Codebase is ready for push and deploy
</success_criteria>

<output>
After completion, create `.planning/phases/01-deploy-everything/01-01-SUMMARY.md`
</output>
