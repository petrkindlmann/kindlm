---
phase: 09-cli-utility-unit-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/cli/src/utils/dry-run.test.ts
  - packages/cli/src/utils/select-reporter.test.ts
  - packages/cli/src/utils/spinner.test.ts
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]

must_haves:
  truths:
    - "formatTestPlan outputs suite name, project, divider, and totals section"
    - "formatTestPlan separates skipped tests from active tests"
    - "formatTestPlan renders command tests with [command] label instead of model ID"
    - "selectReporter returns pretty/json/junit reporters for valid types"
    - "selectReporter calls process.exit(1) on unknown reporter type"
    - "createSpinner start/succeed/fail/stop delegate to ora instance"
    - "createSpinner methods are no-ops when called before start"
  artifacts:
    - path: "packages/cli/src/utils/dry-run.test.ts"
      provides: "formatTestPlan unit tests"
      min_lines: 80
    - path: "packages/cli/src/utils/select-reporter.test.ts"
      provides: "selectReporter unit tests"
      min_lines: 40
    - path: "packages/cli/src/utils/spinner.test.ts"
      provides: "createSpinner unit tests"
      min_lines: 50
  key_links:
    - from: "packages/cli/src/utils/dry-run.test.ts"
      to: "packages/cli/src/utils/dry-run.ts"
      via: "import { formatTestPlan }"
      pattern: "import.*formatTestPlan.*dry-run"
    - from: "packages/cli/src/utils/select-reporter.test.ts"
      to: "packages/cli/src/utils/select-reporter.ts"
      via: "import { selectReporter }"
      pattern: "import.*selectReporter.*select-reporter"
    - from: "packages/cli/src/utils/spinner.test.ts"
      to: "packages/cli/src/utils/spinner.ts"
      via: "import { createSpinner }"
      pattern: "import.*createSpinner.*spinner"
---

<objective>
Add unit test coverage for the three previously-untested CLI utilities: dry-run.ts, select-reporter.ts, and spinner.ts.

Purpose: These are the last untested utilities in @kindlm/cli. Covering them completes the v2.1.0 Gap Closure milestone test requirements.
Output: Three new .test.ts files colocated with their source files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-cli-utility-unit-tests/09-CONTEXT.md

<interfaces>
<!-- TestPlan and TestPlanEntry from @kindlm/core — the input type for formatTestPlan -->

From packages/core/src/engine/test-plan.ts:
```typescript
export interface TestPlanEntry {
  testName: string;
  modelId: string;
  provider: string;
  repeat: number;
  assertionTypes: string[];
  isCommand: boolean;
  skip: boolean;
  tags: string[];
}

export interface TestPlan {
  suiteName: string;
  suiteDescription: string | undefined;
  project: string;
  entries: TestPlanEntry[];
  totalExecutionUnits: number;
  concurrency: number;
  timeoutMs: number;
}
```

From packages/core/src/reporters/interface.ts:
```typescript
export interface Reporter {
  readonly name: string;
  generate(runResult: RunResult, gateEvaluation: GateEvaluation): Promise<ReporterOutput>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unit tests for dry-run.ts and select-reporter.ts</name>
  <files>packages/cli/src/utils/dry-run.test.ts, packages/cli/src/utils/select-reporter.test.ts</files>
  <read_first>
    - packages/cli/src/utils/dry-run.ts
    - packages/cli/src/utils/select-reporter.ts
    - packages/cli/src/utils/env.test.ts (reference: clean describe/it/expect structure)
    - packages/core/src/engine/test-plan.ts (TestPlan + TestPlanEntry interfaces)
    - .planning/phases/09-cli-utility-unit-tests/09-CONTEXT.md (locked decisions on chalk, process.exit mock)
  </read_first>
  <behavior>
    dry-run.test.ts (TEST-01):
    - Test 1: formatTestPlan with 2 active entries → output contains suite name, project name, "Tests to execute:", both test names, totals section with totalExecutionUnits/concurrency/timeoutMs values
    - Test 2: formatTestPlan with 1 active + 1 skip:true entry → output contains "Tests to execute:" with active test, "Skipped:" with skipped test name (dimmed)
    - Test 3: formatTestPlan with isCommand:true entry → output contains "[command]" not "[modelId]"
    - Test 4: formatTestPlan with suiteDescription → output contains the description string
    - Test 5: formatTestPlan with all entries skipped → output contains "No tests to execute."
    - Test 6: formatTestPlan with repeat > 1 → output contains "x3" (or whatever repeat value)
    - Test 7: formatTestPlan with tags → output contains tag strings

    select-reporter.test.ts (TEST-02):
    - Test 1: selectReporter("pretty") → returns object with name "pretty"
    - Test 2: selectReporter("json") → returns object with name "json"
    - Test 3: selectReporter("junit") → returns object with name "junit"
    - Test 4: selectReporter("unknown") → console.error called with message containing "unknown" and available reporters, process.exit(1) called
  </behavior>
  <action>
    Create `packages/cli/src/utils/dry-run.test.ts`:
    - Import { describe, it, expect } from "vitest" and { formatTestPlan } from "./dry-run.js"
    - Import type { TestPlan } from "@kindlm/core"
    - Do NOT mock chalk (per D-02 locked decision). Use `.toContain()` on substrings.
    - Build TestPlan fixtures inline. Minimal fixture: `{ suiteName: "my-suite", suiteDescription: undefined, project: "test-project", entries: [...], totalExecutionUnits: 5, concurrency: 4, timeoutMs: 30000 }`
    - TestPlanEntry fixture: `{ testName: "greeting", modelId: "gpt-4o", provider: "openai", repeat: 1, assertionTypes: ["keywords"], isCommand: false, skip: false, tags: [] }`
    - For skip test: create entry with `skip: true`, assert output contains "Skipped:" and the test name
    - For command test: create entry with `isCommand: true, modelId: "n/a"`, assert output contains "[command]" and does NOT contain "[n/a]"
    - For totals: assert output contains "Total execution units: " and the number, "Concurrency:" and value, "Timeout:" and "ms"

    Create `packages/cli/src/utils/select-reporter.test.ts`:
    - Import { describe, it, expect, vi, beforeEach } from "vitest"
    - Import { selectReporter } from "./select-reporter.js"
    - Mock `process.exit` per D-03 locked decision: `vi.spyOn(process, "exit").mockImplementation(() => { throw new Error("process.exit"); })`
    - Mock `console.error` per D-05: `vi.spyOn(console, "error").mockImplementation(() => {})`
    - In beforeEach: restore mocks with `vi.restoreAllMocks()`, then re-apply spies
    - Test "pretty": `const r = selectReporter("pretty"); expect(r.name).toBe("pretty");`
    - Test "json": `const r = selectReporter("json"); expect(r.name).toBe("json");`
    - Test "junit": `const r = selectReporter("junit"); expect(r.name).toBe("junit");`
    - Test unknown: wrap in try/catch (since process.exit mock throws), assert `process.exit` called with 1, assert `console.error` called with string containing "nope" and "pretty, json, junit"
  </action>
  <verify>
    <automated>cd /Users/petr/projects/kindlm && npx vitest run packages/cli/src/utils/dry-run.test.ts packages/cli/src/utils/select-reporter.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - packages/cli/src/utils/dry-run.test.ts exists with >= 7 test cases in describe("formatTestPlan")
    - packages/cli/src/utils/select-reporter.test.ts exists with >= 4 test cases in describe("selectReporter")
    - All tests pass with exit code 0
    - dry-run tests cover: output format, skipped filtering, command rendering, totals, description, repeat, tags
    - select-reporter tests cover: pretty/json/junit routing, unknown type exit
    - No chalk mocking in dry-run.test.ts (uses .toContain on substrings)
    - process.exit mocked via vi.spyOn with throw pattern in select-reporter.test.ts
  </acceptance_criteria>
  <done>All dry-run and select-reporter tests pass. formatTestPlan tested for output structure, skip filtering, command labels, and totals. selectReporter tested for all 3 valid types and unknown type error path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for spinner.ts</name>
  <files>packages/cli/src/utils/spinner.test.ts</files>
  <read_first>
    - packages/cli/src/utils/spinner.ts
    - packages/cli/src/utils/worktree.test.ts (reference: vi.mock at module level pattern)
    - .planning/phases/09-cli-utility-unit-tests/09-CONTEXT.md (locked decision on ora mock pattern)
  </read_first>
  <behavior>
    spinner.test.ts (TEST-03):
    - Test 1: start(text) → ora called with { text, stream: process.stderr }, .start() called on returned instance
    - Test 2: succeed(text) after start → instance.succeed(text) called
    - Test 3: fail(text) after start → instance.fail(text) called
    - Test 4: stop() after start → instance.stop() called
    - Test 5: succeed() before start → no crash (no-op, no method called on undefined)
    - Test 6: fail() before start → no crash (no-op)
    - Test 7: stop() before start → no crash (no-op)
    - Test 8: after succeed, calling stop is a no-op (instance cleared)
  </behavior>
  <action>
    Create `packages/cli/src/utils/spinner.test.ts`:
    - Import { describe, it, expect, vi, beforeEach } from "vitest"
    - Mock ora at module level per D-04 locked decision:
      ```
      const mockInstance = {
        start: vi.fn().mockReturnThis(),
        succeed: vi.fn(),
        fail: vi.fn(),
        stop: vi.fn(),
      };
      vi.mock("ora", () => ({
        default: vi.fn(() => mockInstance),
      }));
      ```
    - Import ora default after mock: `import ora from "ora";`
    - Import { createSpinner } from "./spinner.js"
    - In beforeEach: `vi.clearAllMocks()` and reset mockReturnThis on start
    - describe("createSpinner"):
      - "start calls ora with text and process.stderr stream": call spinner.start("Loading..."), assert `ora` called with `{ text: "Loading...", stream: process.stderr }`, assert `mockInstance.start` called
      - "succeed delegates to instance.succeed": call start then succeed("Done"), assert `mockInstance.succeed` called with "Done"
      - "fail delegates to instance.fail": call start then fail("Error"), assert `mockInstance.fail` called with "Error"
      - "stop delegates to instance.stop": call start then stop(), assert `mockInstance.stop` called
      - "succeed before start is a no-op": call succeed("Done") without start, assert `mockInstance.succeed` NOT called (ora not called either)
      - "fail before start is a no-op": same pattern
      - "stop before start is a no-op": same pattern
      - "instance cleared after succeed — subsequent stop is no-op": call start, succeed, then stop. Assert stop NOT called (only succeed was called on instance)
  </action>
  <verify>
    <automated>cd /Users/petr/projects/kindlm && npx vitest run packages/cli/src/utils/spinner.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - packages/cli/src/utils/spinner.test.ts exists with >= 8 test cases in describe("createSpinner")
    - All tests pass with exit code 0
    - ora mocked at module level with vi.mock("ora") — no real terminal I/O
    - Tests verify: start delegates to ora + .start(), succeed/fail/stop delegate to instance methods, no-op behavior before start, instance clearing after succeed/fail/stop
    - mockInstance.start uses mockReturnThis() so chaining works
  </acceptance_criteria>
  <done>All spinner tests pass. createSpinner tested for delegation to ora instance methods, no-op safety before start, and instance clearing after succeed/fail/stop.</done>
</task>

</tasks>

<verification>
All three test files pass together:
```bash
cd /Users/petr/projects/kindlm && npx vitest run packages/cli/src/utils/dry-run.test.ts packages/cli/src/utils/select-reporter.test.ts packages/cli/src/utils/spinner.test.ts
```

Full CLI package tests still pass:
```bash
cd /Users/petr/projects/kindlm && npx vitest run --project cli
```

No production code was modified — typecheck is unaffected:
```bash
cd /Users/petr/projects/kindlm && npx tsc --noEmit -p packages/cli/tsconfig.json
```
</verification>

<success_criteria>
- 3 new test files created, colocated with source
- dry-run.test.ts: 7+ tests covering output format, skip filtering, command rendering, description, repeat, tags, totals
- select-reporter.test.ts: 4+ tests covering pretty/json/junit routing and unknown type exit
- spinner.test.ts: 8+ tests covering ora delegation, no-op safety, and instance clearing
- All tests pass with `npx vitest run` (exit code 0)
- Zero production code changes
</success_criteria>

<output>
After completion, create `.planning/phases/09-cli-utility-unit-tests/09-01-SUMMARY.md`
</output>
