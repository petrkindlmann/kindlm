---
"@kindlm/cli": minor
"@kindlm/core": minor
---

KindLM v2.1.0 — `--concurrency` and `--timeout` CLI overrides for `kindlm test`, `betaJudge` multi-pass median scoring flag, `costGating` enforcement flag, `--isolate` now copies config and schema files into worktree.
